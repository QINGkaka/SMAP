# 自动化软件度量平台原理与代码分析说明

## 1. 文档目的

这份文档按照平台前端左侧菜单的实际顺序，对系统的每一个功能进行说明。每个功能都回答四个问题：

1. 这个功能在软件质量保证课程里对应什么知识。
2. 这个功能分析的对象是什么。
3. 这个功能的指标是怎么计算的。
4. 这个功能在代码里是怎么实现的，代码具体在哪里。

这份文档适合直接作为实验报告中“系统原理与实现说明”的基础版本，也适合答辩时逐页讲解。

---

## 2. 平台在课程中的定位

本项目属于实验指导书中的第一类项目，即“开发一个中小型软件度量自动化工具”。它的核心任务不是做一个通用软件开发平台，而是把《软件质量保证》课件中的软件度量方法落地为一个可运行的 Web 系统。

从课程知识看，本项目主要覆盖以下内容：

1. 软件质量、软件测量、软件度量的基本概念。
2. 软件产品度量和软件项目度量。
3. 规模度量：`LoC`、功能点、用例点。
4. 复杂度度量：圈复杂度。
5. 面向对象度量：`CK` 以及部分 `LK` 指标。
6. 设计阶段模型度量：类、接口、属性、操作、继承关系。
7. 预测与估算：工作量、周期、人员、成本。
8. 基于已有结果的智能评价与改进建议。

本系统主要做的是**静态分析和结构化估算**。也就是说，它更关注：

1. 代码有多大。
2. 方法有多复杂。
3. 类之间关系是否合理。
4. 模型文件中设计结构如何。
5. 软件规模、工作量和成本大概是多少。

---

## 3. 平台总体架构

### 3.1 前后端结构

系统采用前后端分离架构：

1. 前端：`Vue`
2. 后端：`Java + Spring Boot`

前端负责：

1. 菜单导航
2. 文件上传
3. 参数录入
4. 结果展示
5. 报告导出

后端负责：

1. 文件存储
2. 度量计算
3. 结果持久化
4. 报告生成
5. 智能分析

### 3.2 左侧菜单顺序

左侧菜单定义在：

[App.vue](./front/src/App.vue)

当前从上到下的顺序是：

1. 项目管理
2. 代码行度量
3. 控制流图度量
4. 面向对象度量
5. 模型文件度量
6. 功能点度量
7. 用例图度量
8. 估算分析
9. 智能分析

本文后面的章节严格按这个顺序展开。

### 3.3 本地文件存储方式

本系统不连接数据库，所有数据保存到本地文件。

核心文件存储服务在：

[FileStorageService.java](./end/src/main/java/com/metriclab/storage/FileStorageService.java)

主要目录结构如下：

1. `metric-data/projects.json`：项目索引
2. `metric-data/projects/{projectId}/uploads/`：上传文件
3. `metric-data/projects/{projectId}/tasks/`：分析结果
4. `metric-data/projects/{projectId}/reports/`：导出报告

项目目录初始化在：

[FileStorageService.java](./end/src/main/java/com/metriclab/storage/FileStorageService.java)

这样设计的好处是：

1. 不依赖 MySQL 之类的数据库环境。
2. 实验环境部署简单。
3. 每个项目的数据、文件和结果相互隔离。
4. 可以直接查看原始 JSON 结果文件。

### 3.4 Java 代码静态分析的公共底座

Java 源代码的复杂度分析和面向对象分析共用一个 AST 支撑类：

[JavaAstSupport.java](./end/src/main/java/com/metriclab/service/JavaAstSupport.java)

这个类的作用是：

1. 使用 JDK Compiler API 把 Java 源码解析成 AST。
2. 提取类、方法、字段、继承、方法调用、字段引用、类型引用。
3. 为复杂度和 CK/LK 指标计算提供结构化输入。

AST 解析入口在：

[JavaAstSupport.java](./end/src/main/java/com/metriclab/service/JavaAstSupport.java)

需要准确说明的一点是：

> 实验指导书建议可以使用 Eclipse ASTParser，而本系统采用的是 JDK Compiler API 实现 AST 优先静态分析。技术路线是一致的，具体实现不是 Eclipse JDT 专用的 ASTParser。

---

## 4. 按页面顺序说明平台功能

## 4.1 项目管理

### 4.1.1 原理

项目管理模块的目标是把所有分析结果与一个具体项目绑定。因为软件度量不能脱离项目上下文进行，否则不同项目的文件、分析结果和报告会混在一起。

课程上虽然不把“项目管理页面”当成一个独立的度量指标，但在工程实现中它是所有度量功能的基础。

### 4.1.2 实现内容

项目管理模块支持：

1. 新建项目
2. 修改项目
3. 删除项目
4. 查看项目任务历史
5. 查看项目已上传文件
6. 上传和删除文件

前端入口主要在：

1. [App.vue](./front/src/App.vue)
2. [ProjectFilePanel.vue](./front/src/components/file/ProjectFilePanel.vue)
3. [useProjectFiles.js](./front/src/composables/useProjectFiles.js)

后端主要在：

1. [ProjectService.java](./end/src/main/java/com/metriclab/service/ProjectService.java)
2. [UploadService.java](./end/src/main/java/com/metriclab/service/UploadService.java)

### 4.1.3 代码逻辑

项目创建逻辑在：

[ProjectService.java](./end/src/main/java/com/metriclab/service/ProjectService.java)

它做的事情是：

1. 校验项目名称。
2. 生成 `project-时间戳-随机串` 形式的项目 ID。
3. 把项目写入 `projects.json`。
4. 创建该项目自己的 `uploads / tasks / reports` 目录。

文件上传逻辑在：

[UploadService.java](./end/src/main/java/com/metriclab/service/UploadService.java)

它支持一次上传多个文件，并且只允许以下后缀：

1. `zip`
2. `java`
3. `oom`
4. `xml`
5. `xmi`

后缀校验在：

[UploadService.java](./end/src/main/java/com/metriclab/service/UploadService.java)

每次上传或删除文件后，系统会清理旧的派生结果，避免分析结果和当前文件不一致。这个逻辑在：

[FileStorageService.java](./end/src/main/java/com/metriclab/storage/FileStorageService.java)

### 4.1.4 这一页和后续度量的关系

后面的所有分析模块都依赖项目管理模块提供的：

1. 项目 ID
2. 上传文件列表
3. 项目目录
4. 最新结果文件位置

因此项目管理模块虽然不直接输出度量值，但它是整个平台的基础功能。

---

## 4.2 代码行度量

### 4.2.1 原理

代码行度量对应课程中的软件规模度量。`LoC` 是最基础的代码规模指标，用来反映代码长度。

本系统具体统计：

1. 总行数 `LoC`
2. 有效代码行 `SLoC`
3. 注释行
4. 空行
5. 注释率

其中：

```text
注释率 = 注释行数 / 总行数
```

### 4.2.2 页面与后端入口

前端页面：

[LocAnalysisView.vue](./front/src/views/LocAnalysisView.vue)

后端入口：

[LocAnalysisService.java](./end/src/main/java/com/metriclab/service/LocAnalysisService.java)

### 4.2.3 代码实现逻辑

LoC 分析主流程是：

1. 先读取项目上传文件列表：[LocAnalysisService.java](./end/src/main/java/com/metriclab/service/LocAnalysisService.java)
2. `.java` 文件直接读取全部行：[LocAnalysisService.java](./end/src/main/java/com/metriclab/service/LocAnalysisService.java)
3. `.zip` 文件逐个条目展开：[LocAnalysisService.java](./end/src/main/java/com/metriclab/service/LocAnalysisService.java)
4. 对每个文件执行 `countLines()`：[LocAnalysisService.java](./end/src/main/java/com/metriclab/service/LocAnalysisService.java)
5. 最后执行项目汇总：[LocAnalysisService.java](./end/src/main/java/com/metriclab/service/LocAnalysisService.java)

### 4.2.4 行分类规则

真正决定一行属于什么类型的逻辑在：

[LocAnalysisService.java](./end/src/main/java/com/metriclab/service/LocAnalysisService.java)

分类规则是：

1. 去除首尾空白后为空，记为空行。
2. 如果处于块注释内部，则本行一般计为注释行。
3. 以 `//` 开头，记为注释行。
4. 以 `/*` 开头且后面没有实际代码，记为注释行。
5. 其余计为有效代码行。

这样做的目的，是把：

1. 空行
2. 纯注释
3. 块注释
4. 含代码的有效行

区分清楚，而不是只做简单字符串计数。

### 4.2.5 ZIP 文件处理

ZIP 中的无效条目过滤逻辑依赖：

[ZipEntryFilter.java](./end/src/main/java/com/metriclab/service/ZipEntryFilter.java)

它会跳过：

1. `__MACOSX/`
2. `._*`
3. `.DS_Store`

这样可以避免把 macOS 打包产生的伪文件误算为 Java 文件。

---

## 4.3 控制流图度量

### 4.3.1 原理

控制流图度量对应课程中的圈复杂度 `Cyclomatic Complexity`。

本系统采用的计算思想是：

```text
CC = 1 + 决策点数量
```

常见决策点包括：

1. `if`
2. `for`
3. `while`
4. `do while`
5. `case`
6. `catch`
7. `?:`
8. `&&`
9. `||`

复杂度越高，说明方法独立执行路径越多，理解、测试和维护难度越大。

### 4.3.2 页面与后端入口

前端页面：

[ComplexityAnalysisView.vue](./front/src/views/ComplexityAnalysisView.vue)

文件范围选择组件：

[MetricFileScopePanel.vue](./front/src/components/metric/MetricFileScopePanel.vue)

后端入口：

[ComplexityAnalysisService.java](./end/src/main/java/com/metriclab/service/ComplexityAnalysisService.java)

### 4.3.3 实现步骤

复杂度分析完整流程如下：

1. 解析分析范围，是整项目还是指定文件：[ComplexityAnalysisService.java](./end/src/main/java/com/metriclab/service/ComplexityAnalysisService.java)
2. 对每个文件执行 `analyzeJavaSource()`：[ComplexityAnalysisService.java](./end/src/main/java/com/metriclab/service/ComplexityAnalysisService.java)
3. 优先尝试 AST 分析：[ComplexityAnalysisService.java](./end/src/main/java/com/metriclab/service/ComplexityAnalysisService.java)
4. 若 AST 无法形成有效结果，则回退到文本扫描：[ComplexityAnalysisService.java](./end/src/main/java/com/metriclab/service/ComplexityAnalysisService.java)
5. 汇总所有方法的复杂度，生成项目级结果：[ComplexityAnalysisService.java](./end/src/main/java/com/metriclab/service/ComplexityAnalysisService.java)

### 4.3.4 AST 路径

如果 AST 解析成功，则方法复杂度直接来自：

[JavaAstSupport.java](./end/src/main/java/com/metriclab/service/JavaAstSupport.java)

AST 复杂度计数器在：

[JavaAstSupport.java](./end/src/main/java/com/metriclab/service/JavaAstSupport.java)

这个计数器会在访问到控制结构节点时累加复杂度。

### 4.3.5 回退文本路径

如果 AST 失败，则系统走文本分析路径。

方法提取逻辑在：

[ComplexityAnalysisService.java](./end/src/main/java/com/metriclab/service/ComplexityAnalysisService.java)

复杂度计数逻辑在：

[ComplexityAnalysisService.java](./end/src/main/java/com/metriclab/service/ComplexityAnalysisService.java)

它的实现方式是：

1. 复杂度初值设为 1。
2. 扫描代码中的 `if/for/while/case/catch`。
3. 再统计 `&&`、`||` 和 `?` 的出现次数。
4. 所有这些计数累计到最终复杂度。

### 4.3.6 风险分级

复杂度风险分级在：

[ComplexityAnalysisService.java](./end/src/main/java/com/metriclab/service/ComplexityAnalysisService.java)

当前分级规则是：

1. `<= 10`：`LOW`
2. `11 ~ 20`：`MEDIUM`
3. `21 ~ 50`：`HIGH`
4. `> 50`：`EXTREME`

---

## 4.4 面向对象度量

### 4.4.1 原理

面向对象度量对应课程中的 `CK` 指标和部分 `LK` 指标。

本系统实现的主要指标有：

1. `WMC`
2. `RFC`
3. `DIT`
4. `NOC`
5. `CBO`
6. `LCOM`
7. `NOA`
8. `NOO`
9. `CS`

它们分别反映：

1. 方法复杂度总量
2. 类响应范围
3. 继承深度
4. 子类数量
5. 类耦合程度
6. 方法内聚程度
7. 属性规模
8. 方法规模
9. 类大小

### 4.4.2 页面与后端入口

前端页面：

[ObjectOrientedView.vue](./front/src/views/ObjectOrientedView.vue)

后端入口：

[ObjectOrientedAnalysisService.java](./end/src/main/java/com/metriclab/service/ObjectOrientedAnalysisService.java)

AST 底座：

[JavaAstSupport.java](./end/src/main/java/com/metriclab/service/JavaAstSupport.java)

### 4.4.3 总体实现流程

面向对象分析流程如下：

1. 先确定待分析文件：[ObjectOrientedAnalysisService.java](./end/src/main/java/com/metriclab/service/ObjectOrientedAnalysisService.java)
2. 对每个 Java 文件执行 `analyzeJavaSource()`：[ObjectOrientedAnalysisService.java](./end/src/main/java/com/metriclab/service/ObjectOrientedAnalysisService.java)
3. 优先使用 AST 路径：[ObjectOrientedAnalysisService.java](./end/src/main/java/com/metriclab/service/ObjectOrientedAnalysisService.java)
4. 得到每个类的原始指标 `RawClassMetric`
5. 再统一补充 `DIT` 和 `NOC`
6. 最终形成 `ClassMetric`

### 4.4.4 WMC

`WMC` 的 AST 实现逻辑在：

[ObjectOrientedAnalysisService.java](./end/src/main/java/com/metriclab/service/ObjectOrientedAnalysisService.java)

实现方式是：

```text
WMC = 当前类中所有可执行方法圈复杂度之和
```

如果一个类没有可执行方法体，则用方法数作为退化值。

### 4.4.5 RFC

`RFC` 的计算入口在：

[ObjectOrientedAnalysisService.java](./end/src/main/java/com/metriclab/service/ObjectOrientedAnalysisService.java)

实际计算在：

[ObjectOrientedAnalysisService.java](./end/src/main/java/com/metriclab/service/ObjectOrientedAnalysisService.java)

实现公式是：

```text
RFC = |本类方法集合 ∪ 本类方法直接调用的方法集合|
```

也就是说：

1. 先把类自己的方法放进集合。
2. 再把这些方法里直接调用到的方法放进集合。
3. 对集合去重。
4. 集合大小就是 RFC。

### 4.4.6 DIT

`DIT` 的计算在：

[ObjectOrientedAnalysisService.java](./end/src/main/java/com/metriclab/service/ObjectOrientedAnalysisService.java)

实现公式是：

```text
DIT = 从当前类沿父类链向上追溯到根类的最大路径长度
```

### 4.4.7 NOC

`NOC` 的统计是在项目级统一建立继承关系后完成的。相关逻辑在：

[ObjectOrientedAnalysisService.java](./end/src/main/java/com/metriclab/service/ObjectOrientedAnalysisService.java)

实现公式是：

```text
NOC = 直接继承当前类的子类数量
```

### 4.4.8 CBO

`CBO` 的实现入口在：

[ObjectOrientedAnalysisService.java](./end/src/main/java/com/metriclab/service/ObjectOrientedAnalysisService.java)

具体计算在：

[ObjectOrientedAnalysisService.java](./end/src/main/java/com/metriclab/service/ObjectOrientedAnalysisService.java)

实现思路是：

1. 收集字段类型引用。
2. 收集方法参数、返回值和局部类型引用。
3. 去掉当前类自身和常见内建类型。
4. 去掉仅由继承关系产生的依赖。
5. 对外部类名称去重后计数。

所以：

```text
CBO = 当前类依赖的不同外部类数量
```

并且：

```text
extends / implements 不直接计入 CBO
```

### 4.4.9 LCOM

`LCOM` 的实现入口在：

[ObjectOrientedAnalysisService.java](./end/src/main/java/com/metriclab/service/ObjectOrientedAnalysisService.java)

核心计算在：

[ObjectOrientedAnalysisService.java](./end/src/main/java/com/metriclab/service/ObjectOrientedAnalysisService.java)

实现方式完全对应课件经典公式：

1. 求每个方法访问到的字段集合。
2. 对任意两个方法：
   - 若字段集合无交集，记 `P++`
   - 若有交集，记 `Q++`
3. 最终：

```text
LCOM = max(P - Q, 0)
```

### 4.4.10 NOA、NOO、CS

这三个类规模相关指标在 AST 路径中统一构造于：

[ObjectOrientedAnalysisService.java](./end/src/main/java/com/metriclab/service/ObjectOrientedAnalysisService.java)

含义分别是：

1. `NOA` = 字段数
2. `NOO` = 方法数
3. `CS` = 类起止行跨度

其中：

```text
CS = endLine - startLine + 1
```

---

## 4.5 模型文件度量

### 4.5.1 原理

模型文件度量对应课程中设计阶段的度量思想。指导书要求系统能够处理类图、用例图、流程图等设计图输入。对自动化工具来说，最适合的输入不是图片，而是结构化模型文件。

本系统支持：

1. `.xml`
2. `.xmi`
3. `.oom`

并从中提取：

1. 类
2. 接口
3. 属性
4. 操作
5. 继承关系

### 4.5.2 页面与后端入口

前端页面：

[ModelAnalysisView.vue](./front/src/views/ModelAnalysisView.vue)

后端入口：

[ModelAnalysisService.java](./end/src/main/java/com/metriclab/service/ModelAnalysisService.java)

### 4.5.3 实现步骤

模型文件分析流程如下：

1. 先筛选模型文件：[ModelAnalysisService.java](./end/src/main/java/com/metriclab/service/ModelAnalysisService.java)
2. 用 DOM 解析 XML：[ModelAnalysisService.java](./end/src/main/java/com/metriclab/service/ModelAnalysisService.java)
3. 扫描所有元素节点，识别类和接口：[ModelAnalysisService.java](./end/src/main/java/com/metriclab/service/ModelAnalysisService.java)
4. 统计属性数量和操作数量：[ModelAnalysisService.java](./end/src/main/java/com/metriclab/service/ModelAnalysisService.java)
5. 提取父类引用：[ModelAnalysisService.java](./end/src/main/java/com/metriclab/service/ModelAnalysisService.java)
6. 统一计算子类数和继承深度：[ModelAnalysisService.java](./end/src/main/java/com/metriclab/service/ModelAnalysisService.java)
7. 汇总项目结果：[ModelAnalysisService.java](./end/src/main/java/com/metriclab/service/ModelAnalysisService.java)

### 4.5.4 结果定义

这一页没有复杂公式，更多是结构统计：

1. 类数量 = `CLASS` 节点数量
2. 接口数量 = `INTERFACE` 节点数量
3. 属性数量 = 所有类属性数之和
4. 操作数量 = 所有类操作数之和
5. 继承关系数量 = 有父类的类数量

所以它属于**设计模型结构度量**，而不是 Java 代码 AST 度量。

---

## 4.6 功能点度量

### 4.6.1 原理

功能点度量对应课程中的 `IFPUG Function Point` 方法。本系统当前实现的是 `IFPUG`，没有展开 `COSMIC`、`Mk II` 等其它体系。

系统支持的五类功能项是：

1. `EI`：外部输入
2. `EO`：外部输出
3. `EQ`：外部查询
4. `ILF`：内部逻辑文件
5. `EIF`：外部接口文件

最终总公式为：

```text
UFP = EI + EO + EQ + ILF + EIF
VAF = 0.65 + 0.01 * GSC
AFP = UFP * VAF
```

### 4.6.2 页面与后端入口

前端页面：

[FunctionPointView.vue](./front/src/views/FunctionPointView.vue)

后端入口：

[FunctionPointService.java](./end/src/main/java/com/metriclab/service/FunctionPointService.java)

### 4.6.3 模式划分

功能点支持两种模式：

1. `DETAILED`：详细计数
2. `ESTIMATED`：预估算

模式判断逻辑在：

[FunctionPointService.java](./end/src/main/java/com/metriclab/service/FunctionPointService.java)

### 4.6.4 详细计数模式

详细计数分析入口在：

[FunctionPointService.java](./end/src/main/java/com/metriclab/service/FunctionPointService.java)

事务功能 `EI/EO/EQ` 分析在：

[FunctionPointService.java](./end/src/main/java/com/metriclab/service/FunctionPointService.java)

数据功能 `ILF/EIF` 分析在：

[FunctionPointService.java](./end/src/main/java/com/metriclab/service/FunctionPointService.java)

复杂度判级逻辑分别在：

1. `EI`：[FunctionPointService.java](./end/src/main/java/com/metriclab/service/FunctionPointService.java)
2. `EO/EQ`：[FunctionPointService.java](./end/src/main/java/com/metriclab/service/FunctionPointService.java)
3. `ILF/EIF`：[FunctionPointService.java](./end/src/main/java/com/metriclab/service/FunctionPointService.java)

当前实现口径是：

1. `EI/EO/EQ` 按 `DET + FTR` 自动判级
2. `ILF/EIF` 按 `DET + RET` 自动判级

### 4.6.5 预估算模式

预估算权重计算在：

[FunctionPointService.java](./end/src/main/java/com/metriclab/service/FunctionPointService.java)

权重公式是：

```text
EI = low*3 + average*4 + high*6
EO = low*4 + average*5 + high*7
EQ = low*3 + average*4 + high*6
ILF = low*7 + average*10 + high*15
EIF = low*5 + average*7 + high*10
```

### 4.6.6 GSC、VAF、AFP

14 个通用系统特征总分的处理在：

[FunctionPointService.java](./end/src/main/java/com/metriclab/service/FunctionPointService.java)

最后总分计算在：

[FunctionPointService.java](./end/src/main/java/com/metriclab/service/FunctionPointService.java)

实现的正是：

```text
UFP = EI + EO + EQ + ILF + EIF
VAF = 0.65 + 0.01 * GSC
AFP = UFP * VAF
```

---

## 4.7 用例图度量

### 4.7.1 原理

用例图度量对应课程中的 `Use Case Point` 方法。

核心中间量有：

1. `UAW`
2. `UUCW`
3. `UUCP`
4. `TCF`
5. `ECF`
6. `UCP`

最终公式为：

```text
UAW = simpleActors*1 + averageActors*2 + complexActors*3
UUCW = simpleUseCases*5 + averageUseCases*10 + complexUseCases*15
UUCP = UAW + UUCW
TCF = 0.6 + 0.01 * TFactor
ECF = 1.4 - 0.03 * EFactor
UCP = UUCP * TCF * ECF
```

### 4.7.2 页面与后端入口

前端页面：

[UseCasePointView.vue](./front/src/views/UseCasePointView.vue)

后端入口：

[UseCasePointService.java](./end/src/main/java/com/metriclab/service/UseCasePointService.java)

### 4.7.3 实现步骤

1. 先计算参与者权重：[UseCasePointService.java](./end/src/main/java/com/metriclab/service/UseCasePointService.java)
2. 再计算用例权重：[UseCasePointService.java](./end/src/main/java/com/metriclab/service/UseCasePointService.java)
3. 求 `UUCP`：[UseCasePointService.java](./end/src/main/java/com/metriclab/service/UseCasePointService.java)
4. 计算技术因子加权总分和环境因子加权总分：[UseCasePointService.java](./end/src/main/java/com/metriclab/service/UseCasePointService.java)
5. 计算 `TCF`、`ECF`、`UCP`：[UseCasePointService.java](./end/src/main/java/com/metriclab/service/UseCasePointService.java)
6. 计算估算工时和估算人月：[UseCasePointService.java](./end/src/main/java/com/metriclab/service/UseCasePointService.java)

### 4.7.4 技术因子和环境因子

技术因子权重数组定义在：

[UseCasePointService.java](./end/src/main/java/com/metriclab/service/UseCasePointService.java)

环境因子权重数组定义在：

[UseCasePointService.java](./end/src/main/java/com/metriclab/service/UseCasePointService.java)

加权求和逻辑在：

[UseCasePointService.java](./end/src/main/java/com/metriclab/service/UseCasePointService.java)

### 4.7.5 工时与人月

本系统默认生产率为：

```text
28 小时 / UCP
```

对应常量定义在：

[UseCasePointService.java](./end/src/main/java/com/metriclab/service/UseCasePointService.java)

并按：

```text
EstimatedHours = UCP * Productivity
EstimatedPersonMonths = EstimatedHours / 160
```

换算工时和人月。

---

## 4.8 估算分析

### 4.8.1 原理

估算分析对应课程中的工作量、开发周期、人员和成本预测。本系统当前使用基础 `COCOMO`。

基本公式为：

```text
Effort = a * (KLOC)^b
Schedule = c * (Effort)^d
AverageStaff = Effort / Schedule
Cost = Effort * costPerPersonMonth
```

### 4.8.2 页面与后端入口

前端页面：

[EstimationView.vue](./front/src/views/EstimationView.vue)

后端入口：

[EstimationService.java](./end/src/main/java/com/metriclab/service/EstimationService.java)

### 4.8.3 实现步骤

1. 先确定估算模式：[EstimationService.java](./end/src/main/java/com/metriclab/service/EstimationService.java)
2. 确定人月成本：[EstimationService.java](./end/src/main/java/com/metriclab/service/EstimationService.java)
3. 解析规模 `KLOC`：[EstimationService.java](./end/src/main/java/com/metriclab/service/EstimationService.java)
4. 计算工作量、周期、平均人员和成本：[EstimationService.java](./end/src/main/java/com/metriclab/service/EstimationService.java)

### 4.8.4 KLOC 来源优先级

规模来源按以下优先级确定：

1. 用户手动输入 `KLOC`
2. 最新 `LoC` 结果自动换算
3. 若没有 LoC，则根据功能点结果换算

这个逻辑在：

[EstimationService.java](./end/src/main/java/com/metriclab/service/EstimationService.java)

功能点换算采用课件中的：

```text
Java 60 SLOC / FP
```

常量定义在：

[EstimationService.java](./end/src/main/java/com/metriclab/service/EstimationService.java)

### 4.8.5 COCOMO 模式参数

三种模式定义在：

[EstimationService.java](./end/src/main/java/com/metriclab/service/EstimationService.java)

分别是：

1. `ORGANIC`
2. `SEMIDETACHED`
3. `EMBEDDED`

每种模式都有自己的 `a、b、c、d` 参数。

---

## 4.9 智能分析

### 4.9.1 原理

智能分析模块不是重新发明新的度量公式，而是在前面各模块结果的基础上做综合解释。

它本质上做的是：

```text
已有度量结果 -> 风险归纳 -> 重构建议 -> 测试建议
```

### 4.9.2 页面与后端入口

前端页面：

[AiAnalysisView.vue](./front/src/views/AiAnalysisView.vue)

后端入口：

[AiAnalysisService.java](./end/src/main/java/com/metriclab/service/AiAnalysisService.java)

### 4.9.3 实现步骤

智能分析主流程是：

1. 读取或补跑 LoC 结果：[AiAnalysisService.java](./end/src/main/java/com/metriclab/service/AiAnalysisService.java)
2. 读取或补跑复杂度结果：[AiAnalysisService.java](./end/src/main/java/com/metriclab/service/AiAnalysisService.java)
3. 读取或补跑面向对象结果：[AiAnalysisService.java](./end/src/main/java/com/metriclab/service/AiAnalysisService.java)
4. 读取或补跑估算结果：[AiAnalysisService.java](./end/src/main/java/com/metriclab/service/AiAnalysisService.java)

然后分成两条路径：

1. 大模型接口可用时，走大模型分析。
2. 大模型接口不可用时，回退到本地规则分析。

### 4.9.4 大模型路径

大模型分析入口在：

[AiAnalysisService.java](./end/src/main/java/com/metriclab/service/AiAnalysisService.java)

提示词构造在：

[AiAnalysisService.java](./end/src/main/java/com/metriclab/service/AiAnalysisService.java)

返回 JSON 解析在：

[AiAnalysisService.java](./end/src/main/java/com/metriclab/service/AiAnalysisService.java)

### 4.9.5 本地规则路径

本地规则分析的核心函数有：

1. 风险项生成：[AiAnalysisService.java](./end/src/main/java/com/metriclab/service/AiAnalysisService.java)
2. 重构建议：[AiAnalysisService.java](./end/src/main/java/com/metriclab/service/AiAnalysisService.java)
3. 测试建议：[AiAnalysisService.java](./end/src/main/java/com/metriclab/service/AiAnalysisService.java)

也就是说，这个模块本身并不改变前面已经算出的数值，而是把这些数值整理成更容易阅读的分析结论。

---

## 5. 平台实现与课程知识的对应关系

如果按左侧菜单顺序来理解，这个平台和课程知识的对应关系可以概括为：

1. 项目管理  
   提供实验数据组织方式，支撑所有度量结果隔离和保存。
2. 代码行度量  
   对应软件规模度量中的 `LoC / SLoC`。
3. 控制流图度量  
   对应传统复杂度度量中的圈复杂度。
4. 面向对象度量  
   对应 `CK` 和部分 `LK` 指标。
5. 模型文件度量  
   对应设计阶段模型结构度量。
6. 功能点度量  
   对应 `IFPUG FP`。
7. 用例图度量  
   对应 `UCP`。
8. 估算分析  
   对应 `COCOMO` 工作量、周期、成本和人员预测。
9. 智能分析  
   对应“利用度量结果进行评价和改进”的应用层分析。

---

## 6. 答辩时建议怎么说明

如果老师问“你们这个系统到底怎么分析的”，可以直接按左侧菜单回答：

1. 项目管理负责组织项目、文件和历史结果。
2. 代码行度量通过逐行分类统计 `LoC / SLoC / 注释行 / 空行`。
3. 控制流图度量优先通过 AST 统计每个方法的圈复杂度。
4. 面向对象度量通过 AST 提取类、方法、字段、调用和继承关系，再计算 `WMC / RFC / DIT / NOC / CBO / LCOM`。
5. 模型文件度量通过解析 `.xml/.xmi/.oom` 提取类、接口、属性、操作和继承关系。
6. 功能点和用例点由用户输入规模参数，后端按课件公式统一计算。
7. 估算分析先确定规模来源，再套用 `COCOMO`。
8. 智能分析在前面所有结果基础上生成质量评价和改进建议。

如果老师继续追问“代码在哪”，可以直接打开本文件中的链接定位到对应实现。这样回答会比较稳，也比较符合实验报告和答辩的表达方式。
