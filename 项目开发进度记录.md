# 项目开发进度记录

## 1. 文档用途

本文档用于记录软件度量自动化工具项目的开发进度，包括已经完成的功能、已经做过的工作、当前存在的问题、后续计划和每次代码修改记录。

后续开始编写代码后，每完成一个功能或一次重要修改，都需要在本文档中补充记录，方便最终整理实验报告、答辩材料和项目总结。

## 2. 当前项目目录状态

当前工作目录：

```text
/Users/xiazixuan/大三下/软件质量保证/exp
```

当前已有目录：

```text
exp
├── front
├── end
├── 软件度量自动化工具实验指导方案.md
└── 项目开发进度记录.md
```

当前说明：

1. `front` 目录用于存放 Vue 前端项目。
2. `end` 目录用于存放 Java 后端项目。
3. `软件度量自动化工具实验指导方案.md` 是项目总体指导方案和设计文档。
4. `项目开发进度记录.md` 是当前开发过程记录文档。

## 3. 已完成工作

### 3.1 阅读和整理实验要求

已根据实验指导书和用户选择的项目方向，明确本项目选择实验类型（一）：

> 开发一个中小型软件度量自动化工具，把软件度量课程和其他相关课程的基本原理和方法应用到软件项目实际开发中。

已确认项目重点要求包括：

1. 实现面向对象度量。
2. 实现 LK 度量模型与 CK 度量模型中的相关度量点。
3. 加入圈复杂度、代码行等传统度量点。
4. 支持软件规模、工作量、成本、开发时间、人员等实体度量。
5. 输入项可以包括类图、流程图、用例图、程序代码和用户输入。
6. Java 程序可使用 ASTParser 或类似 AST 解析工具进行代码统计分析。
7. 项目文档需要规范、完整、内容充实。

### 3.2 编写项目指导方案文档

已在 `exp` 目录下创建并完善：

```text
软件度量自动化工具实验指导方案.md
```

该文档目前已包含以下内容：

1. 实验选题定位。
2. 实验要求对应关系。
3. 系统总体方案。
4. 功能需求设计。
5. 数据需求设计。
6. 非功能需求设计。
7. 度量模型与指标设计。
8. 后端实现指导。
9. 前端实现指导。
10. 自动化度量算法设计。
11. 实现阶段安排。
12. 测试与验证方案。
13. 项目设计与实现报告写作模板。
14. 实验成果清单。
15. 最小可交付版本建议。
16. 验收检查清单。
17. 推荐项目命名。
18. 推荐实现优先级。
19. 报告中的创新点写法。
20. 可直接写入报告的结论参考。

### 3.3 完善前端页面和功能设计

已根据参考图片中的“自动化软件度量平台”界面补充前端设计。

目前设计的主要页面包括：

1. 项目列表页。
2. 项目详情页。
3. 源码上传页。
4. 度量总览页。
5. CK/LK 指标页。
6. 面向对象度量页。
7. 控制流图度量页。
8. 代码行度量页。
9. 功能点度量页。
10. 用例图度量页。
11. 估算分析页。
12. 智能分析页。
13. 报告导出页。

已规划页面整体布局：

```text
顶部标题栏 + 左侧度量菜单 + 中间图表分析区 + 右侧操作面板
```

已规划左侧菜单：

1. 功能点度量。
2. 用例图度量。
3. 面向对象度量。
4. 控制流图度量。
5. 代码行度量。
6. 智能分析。

已规划右侧操作面板：

1. 点击上传。
2. 文件状态显示。
3. 导出 XML 格式。
4. 开始类分析。
5. AI 智能分析。
6. 分析状态提示。

### 3.4 完善面向对象度量设计

已设计面向对象度量页面和雷达图展示方案。

雷达图计划展示的指标包括：

| 指标 | 含义 |
| --- | --- |
| CBO | 类间耦合度 |
| NOO | 操作数量，即类中的方法数量 |
| NOC | 子类数量 |
| NOA | 属性数量 |
| DIT | 继承深度 |
| CS | 类规模 |

已设计类级指标表格字段：

1. 类名。
2. CBO。
3. DIT。
4. NOC。
5. NOA。
6. NOO。
7. CS。
8. WMC。
9. LCOM。
10. 风险等级。

### 3.5 完善大模型智能分析设计

已在指导方案中加入大模型智能分析模块。

已设计的大模型相关内容包括：

1. 大模型接口用途。
2. 后端配置项。
3. 请求数据结构。
4. 提示词模板。
5. 输出格式。
6. 安全边界。
7. 前端智能分析页面。
8. 智能分析结果保存。

大模型模块定位：

1. 不直接参与原始度量计算。
2. 基于系统已经计算出的度量结果生成解释和建议。
3. 用于生成总体质量评价、主要风险、重构建议和测试建议。
4. 默认只发送指标摘要，不发送完整源代码。

### 3.6 完善后端接口设计

已在指导方案中规划后端 REST API。

目前设计的主要接口类别包括：

1. 项目管理接口。
2. 源码上传接口。
3. 模型文件上传接口。
4. 度量任务接口。
5. 指标结果查询接口。
6. 雷达图数据接口。
7. 功能点度量接口。
8. 用例点度量接口。
9. 控制流图分析接口。
10. 项目估算接口。
11. 大模型智能分析接口。
12. XML 导出接口。
13. 报告导出接口。

### 3.7 完善报告写作结构

已在指导方案中规划最终实验报告结构。

报告计划包含：

1. 项目概述。
2. 需求分析。
3. 系统设计。
4. 度量模型与算法。
5. 系统实现。
6. 实验结果与分析。
7. 总结与展望。

报告中可强调的创新点包括：

1. 集成 CK、LK、圈复杂度和规模度量。
2. 基于 AST 自动提取 Java 代码实体。
3. 支持类级、方法级、文件级和项目级分层分析。
4. 支持风险阈值识别。
5. 支持工作量、成本、周期和人员估算。
6. 支持雷达图、表格和报告导出。
7. 接入大模型生成质量分析和改进建议。
8. 支持模型文件上传和 XML 导出。

## 4. 当前尚未开始的编码工作

目前尚未开始实际代码编写。

前端未完成：

1. Vue 项目初始化。
2. 页面路由设计。
3. 顶部标题栏组件。
4. 左侧度量菜单组件。
5. 文件上传组件。
6. 雷达图组件。
7. 类指标表格组件。
8. 功能点度量页面。
9. 用例点度量页面。
10. 智能分析页面。
11. API 请求封装。

后端未完成：

1. Java 后端项目初始化。
2. Spring Boot 基础配置。
3. 本地文件存储目录设计和数据模型类。
4. 项目管理接口。
5. 文件上传接口。
6. Java AST 解析模块。
7. LoC 统计模块。
8. 圈复杂度计算模块。
9. CK 度量模块。
10. LK 度量模块。
11. 功能点和用例点度量模块。
12. 项目估算模块。
13. 大模型接口调用模块。
14. XML 导出模块。
15. 报告导出模块。
16. JSON 文件读写和任务结果保存模块。

## 5. 后续开发计划

推荐按以下顺序实现：

1. 初始化后端 Spring Boot 项目。
2. 初始化前端 Vue 项目。
3. 实现项目管理和文件上传。
4. 实现 Java 文件扫描和基础 LoC 统计。
5. 实现 AST 解析，提取类、方法、字段、继承关系。
6. 实现圈复杂度计算。
7. 实现 CK 度量指标。
8. 实现 LK 度量指标。
9. 实现前端主界面、左侧菜单和上传面板。
10. 实现面向对象雷达图和类指标表格。
11. 实现功能点、用例点、控制流图和代码行度量页面。
12. 实现 COCOMO 工作量、成本、工期和人员估算。
13. 实现 XML 导出。
14. 实现大模型智能分析接口。
15. 实现报告导出。
16. 编写测试样例和实验结果分析。
17. 完善最终实验报告。

## 6. 后续代码修改记录模板

后续每次修改代码后，按照下面格式追加记录。

```text
### YYYY-MM-DD 第 N 次开发记录

本次目标：

完成内容：
1.
2.
3.

修改文件：
1.
2.
3.

已验证：
1.
2.

存在问题：
1.
2.

下一步计划：
1.
2.
```

## 7. 开发记录

### 2026-04-21 第 1 次记录

本次目标：

创建项目指导方案和开发进度记录文档，为后续代码开发和实验报告编写建立基础。

完成内容：

1. 读取实验指导书中的项目要求。
2. 明确选择实验类型（一）：开发中小型软件度量自动化工具。
3. 创建并完善 `软件度量自动化工具实验指导方案.md`。
4. 根据参考图片补充前端页面布局、度量菜单、雷达图和操作面板设计。
5. 补充大模型智能分析接口设计。
6. 创建 `项目开发进度记录.md`。

修改文件：

1. `软件度量自动化工具实验指导方案.md`
2. `项目开发进度记录.md`

已验证：

1. 已确认 `exp` 目录可访问。
2. 已确认 `front` 和 `end` 目录存在。
3. 已确认指导方案文档已生成并完成扩展。

存在问题：

1. `front` 和 `end` 目录目前还没有实际源码。
2. 后端技术选型还需要在开始编码时最终确定，例如 JavaParser 或 Eclipse JDT ASTParser。
3. 大模型 API 的具体服务商、模型名称和 Key 暂未确定。

下一步计划：

1. 初始化后端 Java 项目。
2. 初始化前端 Vue 项目。
3. 实现最小可交付版本中的文件上传、LoC 统计和面向对象度量基础页面。

### 2026-04-21 第 2 次记录

本次目标：

根据“不想使用本地数据库”的要求，将项目设计调整为本地文件存储方案。

完成内容：

1. 明确本项目不连接 MySQL、PostgreSQL、SQLite 等数据库。
2. 将指导方案中的数据库存储调整为本地文件存储。
3. 规划 `metric-data/` 数据目录，用于保存项目、任务、上传文件、度量结果、估算结果、大模型分析结果和报告文件。
4. 将原来的数据库表设计调整为 JSON、XML、Markdown、HTML 文件结构设计。
5. 增加 `project.json` 和 `metrics.json` 示例。
6. 将后端包结构中的 `repository` 调整为 `storage`。
7. 将后端保存流程改为通过 `FileStorageService` 保存 `task.json`、`entities.json`、`metrics.json` 等文件。

修改文件：

1. `软件度量自动化工具实验指导方案.md`
2. `项目开发进度记录.md`

已验证：

1. 已检查指导方案中主要数据库设计内容，已改为本地文件存储方案。
2. 已确认后续编码任务中不再要求数据库表设计，改为本地文件存储目录和数据模型设计。

存在问题：

1. 文件存储的具体目录创建逻辑还未编码实现。
2. JSON 文件并发写入保护暂未设计，实验版本可先按单用户顺序操作实现。

下一步计划：

1. 编码时优先实现 `FileStorageService`。
2. 在后端启动时自动创建 `metric-data/`、`config/`、`projects/` 等目录。
3. 使用 JSON 文件保存项目列表、任务状态和度量结果。

### 2026-04-21 第 3 次记录

本次目标：

搭建前后端基础框架，完成项目的第一阶段代码骨架。

完成内容：

1. 在 `end` 目录下创建 Spring Boot 后端项目结构。
2. 新增 Maven 配置文件 `pom.xml`。
3. 新增后端启动类 `MetricLabApplication`。
4. 新增统一响应结构 `ApiResponse`。
5. 新增本地文件存储配置 `FileStorageProperties`。
6. 新增跨域配置 `WebConfig`。
7. 新增本地文件存储服务 `FileStorageService`，用于初始化 `metric-data`、`config`、`projects` 等目录。
8. 新增健康检查接口 `GET /api/health`。
9. 在 `front` 目录下创建 Vue 3 + Vite 前端项目结构。
10. 新增前端主页面 `App.vue`，完成顶部栏、左侧菜单、中间雷达图占位区、右侧操作面板的基础布局。
11. 新增前端接口封装 `src/services/api.js`，用于调用后端健康检查接口。
12. 新增前端样式文件 `src/styles.css`，完成平台基础视觉风格。

修改文件：

1. `end/pom.xml`
2. `end/src/main/resources/application.yml`
3. `end/src/main/java/com/metriclab/MetricLabApplication.java`
4. `end/src/main/java/com/metriclab/common/ApiResponse.java`
5. `end/src/main/java/com/metriclab/config/FileStorageProperties.java`
6. `end/src/main/java/com/metriclab/config/WebConfig.java`
7. `end/src/main/java/com/metriclab/storage/FileStorageService.java`
8. `end/src/main/java/com/metriclab/controller/HealthController.java`
9. `end/src/main/java/com/metriclab/model/dto/HealthStatus.java`
10. `end/src/test/java/com/metriclab/MetricLabApplicationTests.java`
11. `front/package.json`
12. `front/index.html`
13. `front/vite.config.js`
14. `front/src/main.js`
15. `front/src/App.vue`
16. `front/src/styles.css`
17. `front/src/services/api.js`
18. `项目开发进度记录.md`

已验证：

1. 本地环境存在 Java、Maven、Node 和 npm。
2. 后端 `mvn test` 已通过一次，Spring Boot 上下文可以正常加载。
3. 前端 `npm install` 已完成依赖安装。

存在问题：

1. 前端构建命令尚未由用户手动执行确认。
2. 后端启动命令尚未由用户手动执行确认。
3. 当前前端雷达图仍是静态占位数据，尚未接入真实度量结果。
4. 当前后端只有健康检查和文件存储初始化，尚未实现项目管理、上传和度量计算。

下一步计划：

1. 由用户手动运行前端构建和后端启动命令。
2. 下一个功能优先实现“项目创建与项目列表文件存储”。
3. 后续长时间下载、安装、构建命令由用户手动执行，开发记录中只记录命令和结果。

### 2026-04-21 第 4 次记录

本次目标：

实现第一个业务功能：项目创建、项目列表读取和本地 JSON 文件保存。

完成内容：

1. 后端新增项目创建请求模型 `CreateProjectRequest`。
2. 后端新增项目数据模型 `ProjectInfo`。
3. 后端新增项目索引模型 `ProjectIndex`。
4. 后端新增 `ProjectService`，负责读取 `projects.json`、创建项目、写入项目索引和生成项目目录。
5. 后端新增 `ProjectController`，提供 `GET /api/projects` 和 `POST /api/projects` 接口。
6. 后端新增 `GlobalExceptionHandler`，统一处理参数错误和本地文件读写错误。
7. 扩展 `FileStorageService`，增加项目索引路径、项目目录创建、JSON 读取等能力。
8. 前端扩展 `src/services/api.js`，新增项目列表和项目创建接口封装。
9. 前端在主页面增加项目创建表单，支持输入项目名称、语言和描述。
10. 前端在主页面增加项目列表展示，支持刷新和空状态提示。
11. 前端将“项目数量”指标卡接入真实项目列表数量。
12. 前端补充项目管理区域样式。

修改文件：

1. `end/src/main/java/com/metriclab/storage/FileStorageService.java`
2. `end/src/main/java/com/metriclab/model/dto/CreateProjectRequest.java`
3. `end/src/main/java/com/metriclab/model/dto/ProjectInfo.java`
4. `end/src/main/java/com/metriclab/model/dto/ProjectIndex.java`
5. `end/src/main/java/com/metriclab/service/ProjectService.java`
6. `end/src/main/java/com/metriclab/controller/ProjectController.java`
7. `end/src/main/java/com/metriclab/controller/GlobalExceptionHandler.java`
8. `front/src/services/api.js`
9. `front/src/App.vue`
10. `front/src/styles.css`
11. `项目开发进度记录.md`

已验证：

1. 本次未主动执行构建命令，避免长时间命令输出。
2. 已人工检查接口路径、前端调用路径和 Vue 模板结构。

存在问题：

1. 需要用户手动重启后端和前端，验证项目创建功能。
2. 当前只支持创建和查看项目，尚未支持删除或编辑项目。
3. 当前项目创建后只生成项目目录和 `project.json`，尚未创建度量任务。

下一步计划：

1. 用户运行 `mvn test` 和 `npm run build` 验证本功能。
2. 下一个功能建议实现“源码/模型文件上传”，将文件保存到对应项目的 `uploads/` 目录。

### 2026-04-21 第 5 次记录

本次目标：

实现第二个业务功能：源码/模型文件上传，并将上传文件保存到项目的 `uploads/` 目录。

完成内容：

1. 后端新增上传文件信息模型 `UploadedFileInfo`。
2. 后端新增上传文件索引模型 `UploadIndex`。
3. 后端新增 `UploadService`，负责校验项目、校验文件类型、保存上传文件和维护 `uploads.json`。
4. 后端新增 `UploadController`，提供 `GET /api/projects/{projectId}/files` 和 `POST /api/projects/{projectId}/files` 接口。
5. 扩展 `FileStorageService`，增加上传目录、上传索引路径、文件保存和文件存在判断能力。
6. 上传文件支持 `.zip`、`.java`、`.oom`、`.xml`、`.xmi`。
7. 上传文件会保存为安全生成的文件名，原始文件名记录在 `uploads.json` 中。
8. 前端扩展 `src/services/api.js`，新增文件列表查询和文件上传接口。
9. 前端右侧操作面板新增项目选择框。
10. 前端右侧操作面板新增真实文件选择和保存上传功能。
11. 前端新增已上传文件列表，展示文件名、类型、大小和上传时间。
12. 前端补充上传区域、文件列表和按钮禁用状态样式。

修改文件：

1. `end/src/main/java/com/metriclab/storage/FileStorageService.java`
2. `end/src/main/java/com/metriclab/model/dto/UploadedFileInfo.java`
3. `end/src/main/java/com/metriclab/model/dto/UploadIndex.java`
4. `end/src/main/java/com/metriclab/service/UploadService.java`
5. `end/src/main/java/com/metriclab/controller/UploadController.java`
6. `front/src/services/api.js`
7. `front/src/App.vue`
8. `front/src/styles.css`
9. `项目开发进度记录.md`

已验证：

1. 本次未主动执行构建命令，避免长时间命令输出。
2. 已静态检查上传接口路径、前端调用路径、文件类型限制和模板结构。

存在问题：

1. 需要用户手动重启后端和前端，验证上传功能。
2. 当前只保存上传文件，还没有解压 ZIP 或解析 Java/OOM/XML/XMI 内容。
3. 当前只支持上传记录查看，不支持删除上传文件。

下一步计划：

1. 用户运行 `mvn test` 和 `npm run build` 验证本功能。
2. 下一个功能建议实现“代码行 LoC 统计”，先支持对单个 `.java` 文件进行行数、空行、注释行和有效代码行统计。

### 2026-04-21 第 6 次记录

本次目标：

优化前端可用性，将尚未实现的功能设置为禁用态，避免用户误点。

完成内容：

1. 为左侧菜单增加 `implemented` 状态字段。
2. 保留“面向对象度量”菜单为可用状态。
3. 将功能点度量、用例图度量、控制流图度量、代码行度量、智能分析菜单标记为未完成。
4. 未完成菜单显示深色“未完成”标签，并设置为不可点击。
5. 将“导入示例”按钮设置为未实现禁用态。
6. 将“导出 XML 格式”按钮设置为未实现禁用态。
7. 将“开始类分析”按钮设置为未实现禁用态。
8. 将“AI 智能分析”按钮设置为未实现禁用态。
9. 在右侧操作面板增加提示文案，说明深色按钮表示已规划但尚未完成。
10. 补充未实现菜单、深色禁用按钮和提示文案样式。

修改文件：

1. `front/src/App.vue`
2. `front/src/styles.css`
3. `项目开发进度记录.md`

已验证：

1. 本次未主动执行构建命令，避免长时间命令输出。
2. 已静态检查 Vue 菜单点击逻辑和按钮禁用状态。

存在问题：

1. 需要用户手动运行前端页面，确认禁用态视觉效果。
2. 当前“代码行度量”等功能仍未实现，只是完成了禁用态标识。

下一步计划：

1. 用户运行 `npm run build` 验证前端模板。
2. 下一个功能继续实现“代码行 LoC 统计”。

### 2026-04-21 第 7 次记录

本次目标：

实现第三个业务功能：代码行 LoC 度量，对上传的 `.java` 文件和 `.zip` 中的 Java 文件进行行数统计。

完成内容：

1. 后端新增 `LocSummary`，保存项目级代码行统计汇总。
2. 后端新增 `LocFileMetric`，保存单个 Java 文件的代码行统计结果。
3. 后端新增 `LocAnalysisResult`，保存一次 LoC 分析任务的完整结果。
4. 后端新增 `LocAnalysisService`，负责读取项目上传文件、分析 `.java` 文件、扫描 `.zip` 中的 Java 文件并计算 LoC。
5. 后端新增 `LocAnalysisController`，提供 `POST /api/projects/{projectId}/loc/analyze` 和 `GET /api/projects/{projectId}/loc/latest` 接口。
6. 扩展 `FileStorageService`，增加任务目录、任务结果目录和最新 LoC 结果路径。
7. LoC 统计支持总行数、有效代码行、注释行、空行和注释率。
8. LoC 分析结果保存到 `tasks/{taskId}/loc-result.json`。
9. 最新 LoC 分析结果保存到 `tasks/latest-loc-result.json`，便于前端快速读取。
10. 前端扩展 API，新增 LoC 分析和读取最新 LoC 结果接口。
11. 前端启用左侧“代码行度量”菜单，取消未完成禁用态。
12. 前端新增代码行度量页面，展示汇总卡片和文件明细表格。
13. 前端右侧面板在代码行度量页面显示可用的“开始 LoC 分析”按钮。
14. 前端指标卡在代码行度量页面显示有效代码行和总行数。

修改文件：

1. `end/src/main/java/com/metriclab/storage/FileStorageService.java`
2. `end/src/main/java/com/metriclab/model/dto/LocSummary.java`
3. `end/src/main/java/com/metriclab/model/dto/LocFileMetric.java`
4. `end/src/main/java/com/metriclab/model/dto/LocAnalysisResult.java`
5. `end/src/main/java/com/metriclab/service/LocAnalysisService.java`
6. `end/src/main/java/com/metriclab/controller/LocAnalysisController.java`
7. `front/src/services/api.js`
8. `front/src/App.vue`
9. `front/src/styles.css`
10. `项目开发进度记录.md`

已验证：

1. 本次未主动执行构建命令，避免长时间命令输出。
2. 已静态检查后端接口路径、前端调用路径、菜单启用状态和结果展示字段。

存在问题：

1. 需要用户手动运行 `mvn test` 和 `npm run build` 验证编译。
2. 当前 LoC 统计是规则统计，尚未处理所有极端 Java 注释写法。
3. 当前 `.zip` 只扫描其中的 `.java` 文件，不处理嵌套压缩包。
4. 当前 LoC 只做规模度量，还没有接入 CK/LK 和圈复杂度。

下一步计划：

1. 用户验证 LoC 分析功能。
2. 下一个功能建议实现“圈复杂度统计”，基于 Java 文件中的控制流关键字先做规则版方法复杂度分析。

### 2026-04-21 第 8 次记录

本次目标：

一次性补充多个实用功能，提升项目调试、文件管理和报告导出能力。

完成内容：

1. 后端新增项目删除能力，提供 `DELETE /api/projects/{projectId}` 接口。
2. 后端项目删除会同步更新 `projects.json`，并删除对应项目目录。
3. 后端新增上传文件删除能力，提供 `DELETE /api/projects/{projectId}/files/{fileId}` 接口。
4. 后端上传文件删除会同步更新 `uploads.json`，并删除本地实际上传文件。
5. 后端新增 LoC Markdown 报告结果模型 `LocReportResult`。
6. 后端新增 LoC Markdown 报告导出能力，提供 `GET /api/projects/{projectId}/loc/report` 接口。
7. LoC Markdown 报告会保存到项目 `reports/loc-report.md`，并生成 `loc-report-meta.json`。
8. 前端项目列表新增项目 ID 展示。
9. 前端项目列表新增“复制 ID”按钮，方便使用 curl 调试接口。
10. 前端项目列表支持点击项目来切换当前项目。
11. 前端项目列表新增“删除项目”按钮。
12. 前端上传文件列表新增“删除”按钮。
13. 前端 LoC 页面新增“导出 Markdown”按钮，生成并下载 LoC 报告。
14. 前端补充项目选中态、项目 ID、迷你按钮、危险按钮、上传文件操作区等样式。

修改文件：

1. `end/src/main/java/com/metriclab/storage/FileStorageService.java`
2. `end/src/main/java/com/metriclab/service/ProjectService.java`
3. `end/src/main/java/com/metriclab/controller/ProjectController.java`
4. `end/src/main/java/com/metriclab/service/UploadService.java`
5. `end/src/main/java/com/metriclab/controller/UploadController.java`
6. `end/src/main/java/com/metriclab/model/dto/LocReportResult.java`
7. `end/src/main/java/com/metriclab/service/LocAnalysisService.java`
8. `end/src/main/java/com/metriclab/controller/LocAnalysisController.java`
9. `front/src/services/api.js`
10. `front/src/App.vue`
11. `front/src/styles.css`
12. `项目开发进度记录.md`

已验证：

1. 本次未主动执行构建命令，避免长时间命令输出。
2. 已静态检查项目删除、文件删除、LoC 报告导出接口路径和前端调用路径。

存在问题：

1. 需要用户手动运行 `mvn test` 和 `npm run build` 验证编译。
2. 删除项目会删除该项目在 `metric-data` 下的上传文件和分析结果，使用时需要确认。
3. LoC Markdown 报告目前只包含代码行度量数据，后续可加入复杂度、CK/LK 和大模型分析结论。

下一步计划：

1. 用户验证项目 ID 复制、项目删除、上传文件删除和 LoC Markdown 导出。
2. 下一个功能建议实现“圈复杂度统计”或“代码行报告预览”。

### 2026-04-21 第 9 次记录

本次目标：

一次性实现控制流/圈复杂度分析相关功能，并在前端形成完整可操作页面。

完成内容：

1. 后端新增圈复杂度汇总模型 `ComplexitySummary`。
2. 后端新增方法级复杂度模型 `MethodComplexityMetric`。
3. 后端新增圈复杂度分析结果模型 `ComplexityAnalysisResult`。
4. 后端新增圈复杂度 Markdown 报告模型 `ComplexityReportResult`。
5. 后端新增 `ComplexityAnalysisService`，支持读取项目上传的 `.java` 文件和 `.zip` 中的 Java 文件。
6. 后端实现规则版 Java 方法识别，能够识别单行方法声明和常见多行方法声明。
7. 后端实现 McCabe 圈复杂度统计，基础复杂度为 1，并统计 `if`、`for`、`while`、`case`、`catch`、`switch`、`do`、`&&`、`||`、三目运算符等控制流点。
8. 后端根据圈复杂度阈值输出 `LOW`、`MEDIUM`、`HIGH`、`EXTREME` 风险等级。
9. 后端新增 `POST /api/projects/{projectId}/complexity/analyze` 接口，用于执行圈复杂度分析。
10. 后端新增 `GET /api/projects/{projectId}/complexity/latest` 接口，用于读取最新圈复杂度结果。
11. 后端新增 `GET /api/projects/{projectId}/complexity/report` 接口，用于生成圈复杂度 Markdown 报告。
12. 圈复杂度任务结果保存到 `tasks/{taskId}/complexity-result.json`。
13. 最新圈复杂度结果保存到 `tasks/latest-complexity-result.json`。
14. 圈复杂度 Markdown 报告保存到 `reports/complexity-report.md`。
15. 前端 API 封装新增复杂度分析、读取最新结果、导出报告三个接口。
16. 前端启用左侧“控制流图度量”菜单。
17. 前端新增控制流复杂度分析页面，展示 Java 文件数、方法数、平均复杂度和需关注方法数。
18. 前端新增方法级复杂度表格，展示方法名、文件、来源、行号、圈复杂度和风险等级。
19. 前端新增风险等级彩色标签，低风险、中风险、高风险和极高风险使用不同视觉样式。
20. 前端右侧操作面板在控制流页面显示可用的“开始复杂度分析”按钮。
21. 前端控制流页面新增“导出 Markdown”按钮，支持下载复杂度报告。
22. 上传或删除文件后，前端会提示 LoC 和圈复杂度结果都可以重新分析。

修改文件：

1. `end/src/main/java/com/metriclab/storage/FileStorageService.java`
2. `end/src/main/java/com/metriclab/model/dto/ComplexitySummary.java`
3. `end/src/main/java/com/metriclab/model/dto/MethodComplexityMetric.java`
4. `end/src/main/java/com/metriclab/model/dto/ComplexityAnalysisResult.java`
5. `end/src/main/java/com/metriclab/model/dto/ComplexityReportResult.java`
6. `end/src/main/java/com/metriclab/service/ComplexityAnalysisService.java`
7. `end/src/main/java/com/metriclab/controller/ComplexityAnalysisController.java`
8. `front/src/services/api.js`
9. `front/src/App.vue`
10. `front/src/styles.css`
11. `项目开发进度记录.md`

已验证：

1. 本次未主动执行 `mvn test`、`mvn compile`、`npm run build`，避免占用用户终端和输出 token。
2. 已静态检查后端接口路径、前端 API 调用路径、Vue 模板分支和新增样式。
3. 已确认新增功能仍使用本地文件保存，不需要连接数据库。

存在问题：

1. 当前圈复杂度分析是规则版实现，适合实验展示和中小型样例项目，尚未接入 JavaParser 或 Eclipse JDT ASTParser。
2. 对匿名内部类、lambda、复杂泛型换行声明等极端 Java 写法的识别能力有限。
3. 当前控制流图页面展示的是方法复杂度表格，尚未真正绘制流程图或控制流图。

下一步计划：

1. 用户运行后端和前端构建命令验证本次功能。
2. 下一步建议实现“面向对象 CK/LK 指标规则版分析”，包括类、字段、方法、继承、CBO、DIT、NOC、NOA、NOO、CS、WMC 等基础指标。

### 2026-04-21 第 10 次记录

本次目标：

排查并修复控制流复杂度分析看起来只分析一个文件的问题。

问题原因：

1. 后端原实现确实遍历了当前项目下所有上传的 `.java` 和 `.zip` 文件。
2. 但结果模型只保存“识别到可执行方法体”的文件。
3. 用户上传的多个 Service 文件是 `interface`，只有方法声明，没有 `{ ... }` 方法体。
4. 接口方法声明本身没有控制流分支，因此旧结果中没有显示这些文件，造成“只分析了一个文件”的误解。

完成内容：

1. 后端新增 `ComplexityFileMetric`，用于保存每个 Java 文件的扫描结果。
2. `ComplexityAnalysisResult` 新增 `files` 字段，保存所有被扫描 Java 文件的文件级结果。
3. 圈复杂度分析现在会记录每个 Java 文件的声明方法数、可分析方法体数和扫描状态。
4. `summary.fileCount` 改为统计所有被扫描 Java 文件数量，不再只统计有可执行方法体的文件数量。
5. 对接口或抽象声明文件，结果中显示“未识别到带方法体的可执行方法，通常是接口或抽象声明”。
6. Markdown 报告新增“文件扫描结果”表格。
7. 前端控制流页面新增文件扫描结果表格，展示每个 Java 文件是否被扫描、声明方法数和可分析方法体数。
8. 当前文件如果全部是接口或抽象声明，前端会显示说明：接口和抽象声明没有控制流分支，圈复杂度不单独计算。
9. 收紧声明方法数统计规则，避免将 `System.out.println(...)` 一类方法调用误计为方法声明。

修改文件：

1. `end/src/main/java/com/metriclab/model/dto/ComplexityFileMetric.java`
2. `end/src/main/java/com/metriclab/model/dto/ComplexityAnalysisResult.java`
3. `end/src/main/java/com/metriclab/service/ComplexityAnalysisService.java`
4. `front/src/App.vue`
5. `项目开发进度记录.md`

已验证：

1. 已查看当前项目上传文件，确认多个文件是 `interface`，没有方法体。
2. 已静态检查后端结果构造、Markdown 报告字段和前端展示字段。
3. 本次未主动执行构建命令，仍由用户手动运行验证。

存在问题：

1. 旧的 `latest-complexity-result.json` 仍是旧结构，需要重新点击“开始复杂度分析”生成新结果。
2. 当前规则版分析仍不是完整 AST 解析，复杂 Java 写法后续可升级为 JavaParser 或 Eclipse JDT ASTParser。

下一步计划：

1. 用户重启后端，重新执行控制流复杂度分析，确认 Java 文件数和文件扫描表格。
2. 继续实现 CK/LK 面向对象度量。

### 2026-04-21 第 11 次记录

本次目标：

增加正式的项目管理页面，让用户能够清楚看到每个项目及其对应上传文件，并支持在项目维度上传和删除文件。

完成内容：

1. 前端左侧菜单新增“项目管理”入口。
2. 默认进入系统时打开“项目管理”页面。
3. 项目管理页面保留项目创建表单，可创建项目名称、语言和描述。
4. 项目管理页面展示项目列表，支持选择当前项目、复制项目 ID 和删除项目。
5. 新增项目文件管理看板，按项目分组展示文件。
6. 每个项目卡片展示项目名称、描述、项目 ID、语言、文件数和创建时间。
7. 每个项目卡片内支持单独选择文件并上传到该项目。
8. 每个项目卡片内展示该项目已上传文件，包括文件名、文件类型、文件大小和上传时间。
9. 每个项目卡片内支持删除对应项目下的单个上传文件。
10. 其它度量页面右侧操作面板仍保留当前项目上传入口，保证在不同度量页面也能上传并保存文件。
11. 上传和删除文件后，会同步刷新当前项目文件列表和项目管理页面中的文件映射。
12. 项目管理和度量页面共用后端已有本地文件存储接口，不新增数据库。

修改文件：

1. `front/src/App.vue`
2. `front/src/styles.css`
3. `项目开发进度记录.md`

已验证：

1. 已静态检查新增菜单、项目管理页面状态、项目文件映射和上传/删除函数引用。
2. 已确认文件仍通过后端上传接口保存到 `metric-data/projects/{projectId}/uploads/`。
3. 本次未主动执行 `npm run build`，由用户手动运行验证。

存在问题：

1. 当前项目管理页面使用已有接口分别读取每个项目文件，项目数量很多时可以后续优化为后端聚合接口。
2. 当前项目管理页面主要解决项目和文件关系展示，尚未展示每个项目的历史分析任务和报告文件。

下一步计划：

1. 用户运行前端页面验证项目管理页上传、删除和刷新效果。
2. 后续可增加“任务记录/报告管理”区域，展示每个项目已有的 LoC、复杂度和后续 CK/LK 分析结果。

### 2026-04-21 第 12 次记录

本次目标：

继续排查控制流复杂度仍显示只分析一个文件的问题，并增加旧结果格式提示。

排查结论：

1. 当前项目 `project-20260421171719-e51188b2` 的 `uploads.json` 中确实有 4 个 Java 文件。
2. 最新生成的 `latest-complexity-result.json` 时间为 `2026-04-21T18:05:02`，但仍是旧结构，只包含 `summary` 和 `methods`，没有新增的 `files` 文件扫描明细字段。
3. 当前源码中的 `ComplexityAnalysisResult` 已经包含 `files` 字段，`ComplexityAnalysisService` 也已经使用 `fileMetrics.size()` 统计 Java 文件数。
4. 因此问题不是上传文件丢失，而是当前运行的后端仍然是旧代码，或者后端没有重新编译/重启到最新源码。

完成内容：

1. 前端新增旧版本复杂度结果检测函数 `isLegacyComplexityResult()`。
2. 如果控制流页面读取到缺少 `files` 字段的旧结果，会显示提示：需要重启后端并重新点击“开始复杂度分析”。
3. 新增 warning 提示样式，避免用户把旧 JSON 误认为最新逻辑生成的结果。

修改文件：

1. `front/src/App.vue`
2. `front/src/styles.css`
3. `项目开发进度记录.md`

已验证：

1. 已检查当前上传文件数量和最新复杂度结果结构。
2. 已确认最新复杂度结果仍为旧结构。
3. 已确认源码中已包含新结构字段。

存在问题：

1. 需要用户停止旧后端进程，重新运行后端，让新 Java 源码编译生效。
2. 旧的 `latest-complexity-result.json` 不会自动变成新结构，需要重新执行复杂度分析生成。

下一步计划：

1. 用户重启后端后重新执行复杂度分析。
2. 如果重新分析后仍然没有 `files` 字段，再检查后端启动目录和运行的 classpath。

### 2026-04-21 第 13 次记录

本次目标：

修复后端 Maven 编译失败问题，让复杂度分析新代码可以正常启动。

问题原因：

1. `ComplexityAnalysisService` 中处理 zip 文件时使用了 `units.forEach(unit -> { ... })`。
2. lambda 内引用了 `methodMetrics` 和 `fileMetrics`。
3. 这两个局部变量后续会被重新赋值为排序后的列表，因此不是 Java 要求的 final 或 effectively final。
4. Maven 编译时报错：从 lambda 表达式引用的本地变量必须是最终变量或实际上的最终变量。

完成内容：

1. 将 zip 分析结果处理从 `forEach` lambda 改为普通 `for` 循环。
2. 避免 lambda 捕获后续会重新赋值的局部变量。
3. 说明前端 `ECONNREFUSED ::1:8080` 是后端编译失败没有启动导致，不是前端自身错误。

修改文件：

1. `end/src/main/java/com/metriclab/service/ComplexityAnalysisService.java`
2. `项目开发进度记录.md`

已验证：

1. 已定位 Maven 报错对应的第 58、59 行。
2. 已完成源码修复。
3. 本次未主动运行 Maven，由用户重新执行启动命令验证。

下一步计划：

1. 用户重新运行 `mvn clean spring-boot:run`。
2. 后端启动成功后，前端代理错误会自动消失。
3. 重新执行控制流复杂度分析，确认 `fileCount` 和 `files` 字段。

### 2026-04-21 第 14 次记录

本次目标：

实现面向对象 CK/LK 度量功能，覆盖实验要求中的核心面向对象度量点。

完成内容：

1. 后端新增 `ClassMetric`，用于保存类级 CK/LK 指标。
2. 后端新增 `ObjectOrientedSummary`，用于保存项目级面向对象度量汇总。
3. 后端新增 `ObjectOrientedAnalysisResult`，用于保存一次面向对象分析任务结果。
4. 后端新增 `ObjectOrientedReportResult`，用于保存 Markdown 报告导出结果。
5. 后端新增 `ObjectOrientedAnalysisService`，支持扫描上传的 `.java` 文件和 `.zip` 中的 Java 文件。
6. 后端新增 Java 类/接口/枚举/记录识别逻辑。
7. 后端实现 CK 指标：CBO、DIT、NOC、WMC、LCOM。
8. 后端实现 LK 指标：NOA、NOO、CS。
9. 后端根据 CBO、DIT、WMC、LCOM 阈值输出 `LOW`、`MEDIUM`、`HIGH` 风险等级。
10. 后端新增 `POST /api/projects/{projectId}/oo/analyze` 接口，用于执行 CK/LK 分析。
11. 后端新增 `GET /api/projects/{projectId}/oo/latest` 接口，用于读取最新面向对象度量结果。
12. 后端新增 `GET /api/projects/{projectId}/oo/report` 接口，用于导出面向对象 Markdown 报告。
13. 面向对象分析结果保存到 `tasks/{taskId}/object-oriented-result.json`。
14. 最新面向对象分析结果保存到 `tasks/latest-object-oriented-result.json`。
15. Markdown 报告保存到 `reports/object-oriented-report.md`。
16. 前端 API 封装新增 CK/LK 分析、读取最新结果和导出报告接口。
17. 前端“面向对象度量”页面从静态雷达图占位改为真实 CK/LK 结果页面。
18. 前端新增面向对象汇总卡片：Java 文件数、类/接口数、平均 CBO、风险类数量。
19. 前端新增 CK/LK 指标说明区域。
20. 前端新增类级明细表格，展示类名、类型、文件、CBO、DIT、NOC、NOA、NOO、CS、WMC、LCOM 和风险等级。
21. 前端右侧操作面板在面向对象页面显示“开始 CK/LK 分析”按钮。
22. 前端支持导出面向对象 Markdown 报告。
23. 上传或删除文件后，会提示可重新执行面向对象度量。

修改文件：

1. `end/src/main/java/com/metriclab/storage/FileStorageService.java`
2. `end/src/main/java/com/metriclab/model/dto/ClassMetric.java`
3. `end/src/main/java/com/metriclab/model/dto/ObjectOrientedSummary.java`
4. `end/src/main/java/com/metriclab/model/dto/ObjectOrientedAnalysisResult.java`
5. `end/src/main/java/com/metriclab/model/dto/ObjectOrientedReportResult.java`
6. `end/src/main/java/com/metriclab/service/ObjectOrientedAnalysisService.java`
7. `end/src/main/java/com/metriclab/controller/ObjectOrientedAnalysisController.java`
8. `front/src/services/api.js`
9. `front/src/App.vue`
10. `front/src/styles.css`
11. `项目开发进度记录.md`

已验证：

1. 已静态检查后端接口路径、DTO 字段、文件保存路径和前端 API 调用路径。
2. 已静态检查前端模板中 CK/LK 结果字段和右侧操作按钮。
3. 本次未主动执行 Maven 和 Vite 构建命令，由用户运行验证。

存在问题：

1. 当前 CK/LK 分析为规则版实现，适合实验演示和中小型 Java 样例项目。
2. 对复杂 Java 语法、内部类、泛型嵌套、注解生成方法等场景，后续可升级为 JavaParser 或 Eclipse JDT ASTParser。
3. 当前雷达图暂时替换为结果表格，后续可以把 CK/LK 指标再做成真实雷达图或柱状图。

下一步计划：

1. 用户运行 `mvn clean spring-boot:run` 和前端页面，验证 CK/LK 分析功能。
2. 后续建议实现“大模型智能分析”模块，基于 LoC、圈复杂度、CK/LK 结果生成质量评价和改进建议。

### 2026-04-21 第 15 次记录

本次目标：

继续完善非大模型功能，实现软件规模、工作量、成本、开发时间和人员估算模块。

完成内容：

1. 后端新增 `EstimationRequest`，用于接收估算模式、手动 KLOC 和人月成本。
2. 后端新增 `EstimationResult`，用于保存估算结果。
3. 后端新增 `EstimationReportResult`，用于保存估算 Markdown 报告导出结果。
4. 后端新增 `EstimationService`，实现基础 COCOMO 估算模型。
5. 支持 COCOMO 三种项目模式：有机型、半独立型、嵌入型。
6. 支持用户手动输入 KLOC。
7. 如果用户不输入 KLOC，后端会读取最新 LoC 结果并自动换算 KLOC。
8. 如果当前项目没有 LoC 结果，后端会先自动执行 LoC 分析，再进行估算。
9. 估算结果包括工作量、人月成本、开发周期、平均人员数和估算成本。
10. 后端新增 `POST /api/projects/{projectId}/estimation/analyze` 接口。
11. 后端新增 `GET /api/projects/{projectId}/estimation/latest` 接口。
12. 后端新增 `GET /api/projects/{projectId}/estimation/report` 接口。
13. 估算结果保存到 `tasks/{taskId}/estimation-result.json`。
14. 最新估算结果保存到 `tasks/latest-estimation-result.json`。
15. 估算 Markdown 报告保存到 `reports/estimation-report.md`。
16. 前端新增“估算分析”菜单。
17. 前端新增估算分析页面，支持选择项目模式、输入 KLOC 和人月成本。
18. 前端新增估算结果卡片，展示 KLOC、工作量、开发周期和平均人员。
19. 前端新增估算参数结果区，展示项目模式、规模来源、人月成本和估算成本。
20. 前端支持导出估算 Markdown 报告。
21. 上传或删除文件后，会提示可重新执行估算分析。

修改文件：

1. `end/src/main/java/com/metriclab/storage/FileStorageService.java`
2. `end/src/main/java/com/metriclab/model/dto/EstimationRequest.java`
3. `end/src/main/java/com/metriclab/model/dto/EstimationResult.java`
4. `end/src/main/java/com/metriclab/model/dto/EstimationReportResult.java`
5. `end/src/main/java/com/metriclab/service/EstimationService.java`
6. `end/src/main/java/com/metriclab/controller/EstimationController.java`
7. `front/src/services/api.js`
8. `front/src/App.vue`
9. `front/src/styles.css`
10. `项目开发进度记录.md`

已验证：

1. 已静态检查后端接口路径、DTO 字段、文件保存路径和前端 API 调用路径。
2. 已静态检查前端估算页面、表单字段、结果字段和右侧操作按钮。
3. 本次未主动执行 Maven 和 Vite 构建命令，由用户运行验证。

存在问题：

1. 当前估算模型为基础 COCOMO，适合实验展示和基础预测，不包含复杂成本驱动因子。
2. 如果项目有效代码行非常少，估算结果会偏小，这是 COCOMO 对小样本项目的正常局限。
3. 后续可以加入中级 COCOMO 成本驱动因子、功能点估算和用例点估算进行对比。

下一步计划：

1. 用户运行后端和前端验证估算分析功能。
2. 后续建议继续实现功能点或用例点度量，进一步覆盖实验要求中的其它度量方法。

### 2026-04-21 第 16 次记录

本次目标：

修复 ZIP 文件中 macOS 元数据被误统计的问题，并完善项目文件管理页面的文件展示能力。

完成内容：

1. 后端新增 `ZipEntryFilter`，统一判断 ZIP 内部条目是否为可分析 Java 源文件。
2. 后端排除 ZIP 中的 `__MACOSX/` 目录。
3. 后端排除 ZIP 中的 `._*` 资源分叉元数据文件。
4. 后端排除 ZIP 中的 `.DS_Store` 文件。
5. 代码行度量分析已接入 ZIP 条目过滤逻辑。
6. 控制流圈复杂度分析已接入 ZIP 条目过滤逻辑。
7. 面向对象 CK/LK 分析已接入 ZIP 条目过滤逻辑。
8. 明确 `__MACOSX/src/main/java/.../._CourseStudentController.java` 这类文件不是 Java 源代码，不应该参与统计。
9. 前端项目文件管理列表新增按文件类型分类展示。
10. 前端项目文件管理列表新增文件搜索，支持按文件名、类型和文件 ID 搜索。
11. 前端项目文件管理列表新增排序，支持按上传时间、文件名、类型和大小排序。
12. 前端项目文件管理列表新增升序和降序切换。
13. 前端项目文件管理列表新增分页显示。
14. 前端项目文件管理列表支持手动选择每页 5、10、20 条。
15. 前端分页显示使用安全页码，避免筛选或删除后出现页码越界。
16. 前端在无文件和无匹配结果时分别显示不同空状态。
17. 前端右侧“已上传文件”列表同步支持分类、搜索、排序、分页和每页条数选择。

修改文件：

1. `end/src/main/java/com/metriclab/service/ZipEntryFilter.java`
2. `end/src/main/java/com/metriclab/service/LocAnalysisService.java`
3. `end/src/main/java/com/metriclab/service/ComplexityAnalysisService.java`
4. `end/src/main/java/com/metriclab/service/ObjectOrientedAnalysisService.java`
5. `front/src/App.vue`
6. `front/src/styles.css`
7. `项目开发进度记录.md`

已验证：

1. 已静态检查 ZIP 过滤逻辑的接入位置。
2. 已静态检查前端文件列表分类、搜索、排序、分页和页码边界逻辑。
3. 本次未主动执行 Maven 和 Vite 构建命令，由用户运行验证。

存在问题：

1. 旧的分析结果文件不会自动重算，用户需要重新点击 LoC、圈复杂度或 CK/LK 分析按钮生成新结果。
2. 当前过滤规则主要面向常见 macOS ZIP 元数据，后续如果遇到更多无效文件类型，可以继续加入黑名单规则。

下一步计划：

1. 用户重启后端并刷新前端，验证 ZIP 元数据文件不再被统计。
2. 后续继续完善其它非大模型功能，例如功能点度量或用例点估算。

### 2026-04-21 第 17 次记录

本次目标：

实现指导方案第 7 阶段中的 XML 度量结果导出能力，把已有 LoC、圈复杂度、CK/LK 和估算结果统一归档为 XML 文件。

完成内容：

1. 后端新增 `XmlExportResult`，用于返回 XML 导出路径、内容和导出时间。
2. 后端新增 `XmlExportService`，统一读取或自动生成 LoC、圈复杂度、CK/LK 和 COCOMO 估算结果。
3. 后端新增 `GET /api/projects/{projectId}/export/xml` 接口。
4. XML 文件包含项目 ID、代码行汇总、文件明细、方法圈复杂度、类级 CK/LK 指标和估算结果。
5. XML 导出结果保存到 `metric-data/projects/{projectId}/reports/metrics.xml`。
6. 前端 API 封装新增 `exportProjectXml()`。
7. 前端右侧“导出 XML 格式”按钮从占位状态改为可点击真实导出。
8. 前端支持将后端生成的 XML 内容下载为 `metrics.xml`。
9. 前端新增 XML 导出成功和失败提示。

修改文件：

1. `end/src/main/java/com/metriclab/model/dto/XmlExportResult.java`
2. `end/src/main/java/com/metriclab/service/XmlExportService.java`
3. `end/src/main/java/com/metriclab/controller/XmlExportController.java`
4. `front/src/services/api.js`
5. `front/src/App.vue`
6. `项目开发进度记录.md`

已验证：

1. 已静态检查 XML 标签转义逻辑，避免类名、文件名中的特殊字符破坏 XML 结构。
2. 已静态检查前端导出按钮、API 路径和下载逻辑。

存在问题：

1. XML 导出依赖当前项目存在可分析 Java 文件；如果项目未上传 `.java` 或包含 Java 文件的 `.zip`，后端会返回提示。
2. XML 当前为实验归档格式，未引入 XSD 约束文件。

下一步计划：

1. 实现本地规则版智能分析，在没有外部大模型 Key 的情况下也能生成质量评价和改进建议。
2. 将智能分析结果保存为 JSON 和 Markdown，便于前端展示和最终报告引用。

### 2026-04-21 第 18 次记录

本次目标：

实现指导方案第 8 阶段中的智能分析功能，并提供不依赖外部大模型 API 的本地规则兜底版本。

完成内容：

1. 后端新增 `AiAnalysisResult`，用于保存智能分析任务、总体评价、风险项、重构建议、测试建议和 Markdown 内容。
2. 后端新增 `AiAnalysisService`，基于 LoC、圈复杂度、CK/LK 和 COCOMO 估算结果生成质量分析。
3. 智能分析会在缺少基础度量结果时自动触发 LoC、圈复杂度、CK/LK 和估算分析。
4. 智能分析结果保存到 `tasks/{taskId}/ai-analysis.json`。
5. 智能分析 Markdown 保存到 `tasks/{taskId}/ai-analysis.md` 和 `reports/ai-analysis.md`。
6. 最新智能分析结果保存到 `tasks/latest-ai-analysis-result.json`。
7. 后端新增 `POST /api/projects/{projectId}/ai-analysis/analyze` 接口。
8. 后端新增 `GET /api/projects/{projectId}/ai-analysis/latest` 接口。
9. 前端 API 封装新增智能分析执行和最新结果读取函数。
10. 前端“智能分析”菜单从未实现状态改为可点击状态。
11. 前端新增智能分析结果页面，展示总体评价、主要风险、重构建议和测试建议。
12. 前端支持导出智能分析 Markdown 文件。
13. 右侧操作面板的“AI 智能分析”按钮改为真实执行本地规则智能分析。

修改文件：

1. `end/src/main/java/com/metriclab/storage/FileStorageService.java`
2. `end/src/main/java/com/metriclab/model/dto/AiAnalysisResult.java`
3. `end/src/main/java/com/metriclab/service/AiAnalysisService.java`
4. `end/src/main/java/com/metriclab/controller/AiAnalysisController.java`
5. `front/src/services/api.js`
6. `front/src/App.vue`
7. `front/src/styles.css`
8. `项目开发进度记录.md`

已验证：

1. 已静态检查智能分析自动读取和生成基础度量结果的调用链。
2. 已静态检查前端智能分析菜单、按钮、结果字段和 Markdown 下载逻辑。

存在问题：

1. 当前智能分析为本地规则生成，不会调用真实外部大模型。
2. 后续如果接入真实大模型 API，需要增加模型配置、密钥配置、异常兜底和提示词模板管理。

下一步计划：

1. 实现综合 Markdown 报告导出，把 LoC、复杂度、CK/LK、估算和智能分析结果合并为一份实验报告素材。
2. 运行 Maven 测试和前端构建，检查本轮新增功能是否存在编译或打包问题。

### 2026-04-21 第 19 次记录

本次目标：

实现指导方案第 9 阶段中的综合报告导出能力，并对本轮新增功能进行构建验证。

完成内容：

1. 后端新增 `ComprehensiveReportResult`，用于返回综合报告路径、内容和导出时间。
2. 后端新增 `ComprehensiveReportService`，汇总 LoC、圈复杂度、CK/LK、COCOMO 估算和智能分析结果。
3. 综合报告会在缺少基础结果时自动触发对应分析，保证报告内容完整。
4. 综合报告保存到 `metric-data/projects/{projectId}/reports/final-metric-report.md`。
5. 后端新增 `GET /api/projects/{projectId}/report/comprehensive` 接口。
6. 前端 API 封装新增 `exportComprehensiveReport()`。
7. 前端右侧操作面板新增“导出综合报告”按钮。
8. 前端支持下载 `final-metric-report.md`。
9. 综合报告内容覆盖项目概述、规模度量、圈复杂度、CK/LK 面向对象度量、工作量成本估算、智能分析结论和总结展望。

修改文件：

1. `end/src/main/java/com/metriclab/model/dto/ComprehensiveReportResult.java`
2. `end/src/main/java/com/metriclab/service/ComprehensiveReportService.java`
3. `end/src/main/java/com/metriclab/controller/ComprehensiveReportController.java`
4. `front/src/services/api.js`
5. `front/src/App.vue`
6. `项目开发进度记录.md`

已验证：

1. 后端已执行 `mvn test`，结果为 BUILD SUCCESS，测试 1 个，通过 1 个。
2. 前端已执行 `npm install` 安装依赖。
3. 前端已执行 `npm run build`，Vite 构建成功，生成 `dist/index.html`、CSS 和 JS 资源。

存在问题：

1. 首次在沙箱内运行 Maven 时因无权写入本机 Maven 仓库失败，已在用户批准后于沙箱外成功执行。
2. 首次前端构建因缺少 `node_modules` 和 esbuild 子进程权限失败，已在用户批准后完成依赖安装和构建。
3. `npm install` 提示存在 2 个中等风险依赖漏洞，当前未执行 `npm audit fix --force`，避免引入破坏性依赖升级。

下一步计划：

1. 用户可以启动后端和前端，在页面中验证 XML 导出、智能分析和综合报告导出。
2. 后续如需继续扩展，可补充真实大模型 API 配置、功能点度量、用例点估算和 UML/XMI 模型解析。

### 2026-04-21 第 20 次记录

本次目标：

实现指导方案中“功能点度量模块”，补齐前端菜单中已规划但未实现的功能点度量入口。

完成内容：

1. 后端新增 `FunctionPointCount`，用于保存低、中、高复杂度数量。
2. 后端新增 `FunctionPointRequest`，用于接收 EI、EO、EQ、ILF、EIF 和通用系统特征总分。
3. 后端新增 `FunctionPointResult`，用于保存未调整功能点 UFP、调整因子 VAF 和调整后功能点 AFP。
4. 后端新增 `FunctionPointReportResult`，用于返回功能点 Markdown 报告。
5. 后端新增 `FunctionPointService`，按 IFPUG 常见权重计算功能点。
6. 支持 EI 权重 3、4、6；EO 权重 4、5、7；EQ 权重 3、4、6；ILF 权重 7、10、15；EIF 权重 5、7、10。
7. 支持通用系统特征总分 0 到 70，并按 `VAF = 0.65 + 0.01 × GSC` 计算调整因子。
8. 功能点结果保存到 `tasks/{taskId}/function-point-result.json`。
9. 最新功能点结果保存到 `tasks/latest-function-point-result.json`。
10. 功能点 Markdown 报告保存到 `reports/function-point-report.md`。
11. 后端新增 `POST /api/projects/{projectId}/function-point/analyze` 接口。
12. 后端新增 `GET /api/projects/{projectId}/function-point/latest` 接口。
13. 后端新增 `GET /api/projects/{projectId}/function-point/report` 接口。
14. 前端 API 封装新增功能点分析、读取最新结果和导出报告函数。
15. 前端“功能点度量”菜单从未实现状态改为可点击状态。
16. 前端新增 EI、EO、EQ、ILF、EIF 的低/中/高复杂度录入表单。
17. 前端新增通用系统特征总分录入。
18. 前端新增 UFP、VAF、AFP、GSC 总分和各类功能点结果展示。
19. 前端支持导出功能点 Markdown 报告。

修改文件：

1. `end/src/main/java/com/metriclab/storage/FileStorageService.java`
2. `end/src/main/java/com/metriclab/model/dto/FunctionPointCount.java`
3. `end/src/main/java/com/metriclab/model/dto/FunctionPointRequest.java`
4. `end/src/main/java/com/metriclab/model/dto/FunctionPointResult.java`
5. `end/src/main/java/com/metriclab/model/dto/FunctionPointReportResult.java`
6. `end/src/main/java/com/metriclab/service/FunctionPointService.java`
7. `end/src/main/java/com/metriclab/controller/FunctionPointController.java`
8. `front/src/services/api.js`
9. `front/src/App.vue`
10. `front/src/styles.css`
11. `项目开发进度记录.md`

已验证：

1. 已静态检查功能点权重、VAF 计算和结果保存路径。
2. 已静态检查前端功能点表单字段、API 调用和结果展示字段。

存在问题：

1. 当前功能点度量需要用户手动录入功能项数量，暂未从需求文档或接口定义中自动提取。
2. GSC 使用总分录入方式，暂未展开 14 个通用系统特征逐项评分。

下一步计划：

1. 继续实现用例点估算模块，补齐“用例图度量”菜单。
2. 后续可把功能点和用例点结果加入综合报告与 XML 导出。

### 2026-04-21 第 21 次记录

本次目标：

实现指导方案中“用例图度量/用例点估算模块”，补齐前端菜单中已规划但未实现的用例图度量入口。

完成内容：

1. 后端新增 `UseCasePointRequest`，用于接收参与者数量、用例数量、技术因子、环境因子和生产率参数。
2. 后端新增 `UseCasePointResult`，用于保存 UAW、UUCW、UUCP、TCF、ECF、UCP、估算工时和估算人月。
3. 后端新增 `UseCasePointReportResult`，用于返回用例点 Markdown 报告。
4. 后端新增 `UseCasePointService`，实现用例点估算算法。
5. 参与者权重支持简单 1、一般 2、复杂 3。
6. 用例权重支持简单 5、一般 10、复杂 15。
7. 支持 `TCF = 0.6 + 0.01 × 技术因子总分`。
8. 支持 `ECF = 1.4 - 0.03 × 环境因子总分`。
9. 支持 `UCP = UUCP × TCF × ECF`。
10. 支持按小时/用例点估算总工时，并按 160 小时/人月换算估算人月。
11. 用例点结果保存到 `tasks/{taskId}/use-case-point-result.json`。
12. 最新用例点结果保存到 `tasks/latest-use-case-point-result.json`。
13. 用例点 Markdown 报告保存到 `reports/use-case-point-report.md`。
14. 后端新增 `POST /api/projects/{projectId}/use-case-point/analyze` 接口。
15. 后端新增 `GET /api/projects/{projectId}/use-case-point/latest` 接口。
16. 后端新增 `GET /api/projects/{projectId}/use-case-point/report` 接口。
17. 前端 API 封装新增用例点分析、读取最新结果和导出报告函数。
18. 前端“用例图度量”菜单从未实现状态改为可点击状态。
19. 前端新增参与者复杂度、用例复杂度和调整参数录入表单。
20. 前端新增 UCP、估算工时、估算人月、UUCP、UAW、UUCW、TCF 和 ECF 结果展示。
21. 前端支持导出用例点 Markdown 报告。

修改文件：

1. `end/src/main/java/com/metriclab/storage/FileStorageService.java`
2. `end/src/main/java/com/metriclab/model/dto/UseCasePointRequest.java`
3. `end/src/main/java/com/metriclab/model/dto/UseCasePointResult.java`
4. `end/src/main/java/com/metriclab/model/dto/UseCasePointReportResult.java`
5. `end/src/main/java/com/metriclab/service/UseCasePointService.java`
6. `end/src/main/java/com/metriclab/controller/UseCasePointController.java`
7. `front/src/services/api.js`
8. `front/src/App.vue`
9. `front/src/styles.css`
10. `项目开发进度记录.md`

已验证：

1. 已静态检查用例点估算公式、默认参数和结果保存路径。
2. 已静态检查前端用例点表单字段、API 调用和结果展示字段。

存在问题：

1. 当前用例点估算需要用户手动录入参与者和用例复杂度数量，暂未自动解析 UML 用例图。
2. 技术因子和环境因子当前采用总分录入方式，暂未展开逐项加权评分。

下一步计划：

1. 将功能点和用例点结果纳入 XML 导出和综合报告。
2. 增强面向对象度量页面，增加真实 CK/LK 雷达图展示。

### 2026-04-21 第 22 次记录

本次目标：

完善导出与可视化能力，将新增的功能点和用例点结果接入已有归档输出，并增强面向对象度量页面的雷达图展示。

完成内容：

1. XML 导出服务新增功能点结果读取。
2. XML 导出服务新增用例点结果读取。
3. 如果当前项目已经执行过功能点度量，`metrics.xml` 会包含 `<functionPoint>` 节点。
4. 如果当前项目已经执行过用例点估算，`metrics.xml` 会包含 `<useCasePoint>` 节点。
5. 综合 Markdown 报告新增功能点度量章节。
6. 综合 Markdown 报告新增用例点估算章节。
7. 面向对象度量页面新增真实 CK/LK 雷达图展示。
8. 雷达图维度包括 CBO、NOO、NOC、NOA、DIT 和 CS。
9. 雷达图自动选择综合风险较高的前 4 个类进行展示。
10. 雷达图数据来自后端 CK/LK 分析结果，不再使用静态占位数据。
11. 前端新增雷达图图例颜色映射。

修改文件：

1. `end/src/main/java/com/metriclab/service/XmlExportService.java`
2. `end/src/main/java/com/metriclab/service/ComprehensiveReportService.java`
3. `front/src/App.vue`
4. `front/src/styles.css`
5. `项目开发进度记录.md`

已验证：

1. 已静态检查 XML 中功能点和用例点节点的可选输出逻辑。
2. 已静态检查综合报告中功能点和用例点章节的条件输出逻辑。
3. 已静态检查雷达图数据归一化、排序和 SVG points 生成逻辑。

存在问题：

1. 雷达图当前使用原生 SVG 实现，未引入 ECharts；优点是依赖少，后续如需更丰富交互可替换为 ECharts。
2. 功能点和用例点结果只有在用户执行对应分析后才会进入 XML 和综合报告，不会强制自动生成默认结果。

下一步计划：

1. 运行后端 `mvn test` 和前端 `npm run build`，验证本轮新增功能。
2. 根据构建结果修复可能出现的编译或模板问题。

### 2026-04-21 第 23 次记录

本次目标：

对第 20 至第 22 次新增的功能点度量、用例点估算、导出增强和雷达图展示进行构建验证。

完成内容：

1. 执行后端 Maven 测试，验证新增 Controller、Service 和 DTO 能够正常编译并加载 Spring Boot 上下文。
2. 执行前端 Vite 构建，验证新增菜单、表单、结果页、雷达图和 API 封装没有模板或打包错误。

修改文件：

1. `项目开发进度记录.md`

已验证：

1. 后端执行 `mvn test` 成功。
2. 后端测试结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0。
3. 前端执行 `npm run build` 成功。
4. 前端构建生成 `dist/index.html`、CSS 和 JS 资源。

存在问题：

1. Maven 测试输出中包含 Spring Boot 自动配置匹配日志，属于当前日志级别下的正常输出。
2. 当前尚未进行浏览器端手工点击验证，需要用户启动前后端后在页面中逐项操作确认。

下一步计划：

1. 用户启动后端和前端，验证功能点度量、用例点估算、CK/LK 雷达图、XML 导出和综合报告导出。
2. 后续可继续扩展 UML/XMI 自动解析、逐项 GSC/TCF/ECF 评分和 ECharts 图表交互。

### 2026-04-21 第 24 次记录

本次目标：

接入豆包/火山方舟大模型 API 能力，让“智能分析”模块可以在配置真实大模型服务后生成质量评价和改进建议。

完成内容：

1. 新增 `LargeModelProperties` 配置类，用于读取大模型开关、服务商、接口地址、API Key、模型/接入点 ID 和超时时间。
2. 后端启动类新增大模型配置类绑定。
3. `application.yml` 新增火山方舟兼容 OpenAI Chat Completions 的默认接口地址。
4. 支持通过环境变量 `LARGE_MODEL_ENABLED` 开启或关闭真实大模型调用。
5. 支持通过环境变量 `ARK_API_KEY` 或 `LARGE_MODEL_API_KEY` 读取 API Key。
6. 支持通过环境变量 `ARK_MODEL` 或 `LARGE_MODEL_MODEL` 读取模型名或火山方舟推理接入点 ID。
7. `AiAnalysisService` 新增外部大模型调用逻辑。
8. 智能分析会把 LoC、圈复杂度、CK/LK、COCOMO、功能点和用例点摘要组织成 JSON 提示词。
9. 大模型返回要求为结构化 JSON，字段包括总体评价、风险项、重构建议和测试建议。
10. 后端支持解析大模型返回的 JSON 内容并保存到智能分析结果中。
11. 如果未配置大模型、接口调用失败或返回内容无法解析，系统自动回退到本地规则版智能分析。
12. API Key 未写入代码仓库，避免敏感信息泄露。

修改文件：

1. `end/src/main/java/com/metriclab/config/LargeModelProperties.java`
2. `end/src/main/java/com/metriclab/MetricLabApplication.java`
3. `end/src/main/resources/application.yml`
4. `end/src/main/java/com/metriclab/service/AiAnalysisService.java`
5. `项目开发进度记录.md`

已验证：

1. 后端执行 `mvn test` 成功。
2. 后端测试结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0。
3. 前端执行 `npm run build` 成功。

存在问题：

1. 当前代码已支持豆包/火山方舟调用，但实际调用还需要用户在运行环境中配置 API Key 和模型/接入点 ID。
2. 用户已经在对话中提供过 API Key，建议后续在火山方舟控制台重新生成 Key，并将旧 Key 停用，避免密钥泄露风险。

下一步计划：

1. 用户配置环境变量后启动后端，在前端“智能分析”页面点击“AI 智能分析”验证真实豆包返回内容。
2. 如需保留验收证据，可截图智能分析结果页面和后端运行日志中的请求成功时间。

### 2026-04-21 第 25 次记录

本次目标：

根据用户已开通的 Doubao-Seed-1.8 模型信息，完善豆包大模型运行配置，并新增 `.env` 与 `env.example` 配置文件。

完成内容：

1. `.gitignore` 新增 `.env` 和 `.env.local`，避免真实 API Key 被提交到 Git。
2. 新增 `.env` 文件，用于保存本地真实大模型运行配置。
3. 新增 `env.example` 文件，用于保存不含真实密钥的环境变量模板。
4. `.env` 中配置 `LARGE_MODEL_ENABLED=true`，启用真实大模型调用。
5. `.env` 中配置火山方舟 Chat Completions 接口地址。
6. `.env` 中配置 `ARK_MODEL=doubao-seed-1-8-251228`。
7. `.env` 中配置 `ARK_REASONING_EFFORT=medium`。
8. `.env` 中配置 `ARK_MAX_COMPLETION_TOKENS=4096`。
9. 后端启动类新增 `.env` 加载逻辑，支持从当前目录或父级目录读取 `.env`。
10. `.env` 加载逻辑不会覆盖操作系统中已经存在的同名环境变量。
11. 大模型配置类新增 `reasoningEffort` 和 `maxCompletionTokens` 字段。
12. `application.yml` 新增 `ARK_REASONING_EFFORT`、`ARK_MAX_COMPLETION_TOKENS` 和超时时间配置。
13. `AiAnalysisService` 调用火山方舟时会传入 `reasoning_effort`。
14. `AiAnalysisService` 调用火山方舟时会传入 `max_completion_tokens`。

修改文件：

1. `.gitignore`
2. `.env`
3. `env.example`
4. `end/src/main/java/com/metriclab/config/LargeModelProperties.java`
5. `end/src/main/java/com/metriclab/MetricLabApplication.java`
6. `end/src/main/resources/application.yml`
7. `end/src/main/java/com/metriclab/service/AiAnalysisService.java`
8. `项目开发进度记录.md`

已验证：

1. 后端执行 `mvn test` 成功。
2. 后端测试结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0。
3. 前端执行 `npm run build` 成功。

存在问题：

1. `.env` 中包含真实 API Key，仅适合本地运行，不应提交或发送给他人。
2. 用户此前已经在对话中暴露 API Key，建议后续在火山方舟控制台重新生成 Key 并停用旧 Key。
3. 本次验证只覆盖编译和上下文启动，尚未实际向火山方舟发起智能分析请求。

下一步计划：

1. 启动后端和前端，在页面中点击“AI 智能分析”，验证 Doubao-Seed-1.8 实际返回内容。
2. 如需写入实验报告，可截图 `.env` 模板、智能分析页面和生成的 AI 分析 Markdown 结果。

### 2026-04-21 第 26 次记录

本次目标：

继续补齐指导方案中尚未 100% 完成的模型文件度量、详细评分输入和报告导出闭环。

完成内容：

1. 新增模型文件度量后端能力，支持扫描 `.xml`、`.xmi`、`.oom` 上传文件。
2. 模型文件解析支持识别 UML 类、接口、属性、操作和继承关系。
3. 模型度量结果包含模型文件数、类数量、接口数量、属性数量、操作数量、继承关系数量和高风险类数量。
4. 模型类级明细包含类/接口名称、来源文件、属性数、操作数、子类数、继承深度、类规模、父类和风险等级。
5. 新增模型文件度量接口，支持执行分析、读取最新结果和导出 Markdown 报告。
6. 前端新增“模型文件度量”菜单、分析按钮、摘要卡片、明细表格和 Markdown 导出入口。
7. 功能点度量新增 14 项 GSC 明细评分输入，每项 0-5 分，并自动汇总通用系统特征总分。
8. 用例点估算新增 13 项 TCF 技术复杂度因子评分输入，每项 0-5 分，并自动汇总技术因子总分。
9. 用例点估算新增 8 项 ECF 环境复杂度因子评分输入，每项 0-5 分，并自动汇总环境因子总分。
10. 后端功能点和用例点接口支持接收明细评分数组，同时保留总分字段兼容旧请求。
11. XML 导出新增模型文件度量结果节点，综合输出源码度量、估算度量、功能点、用例点和模型度量数据。
12. 综合 Markdown 报告新增模型文件度量章节，并将总结内容更新为当前已完成能力。
13. 修复模型解析中带命名空间的 `Attribute`、`Operation` 标签识别问题。

修改文件：

1. `end/src/main/java/com/metriclab/model/dto/ModelClassMetric.java`
2. `end/src/main/java/com/metriclab/model/dto/ModelAnalysisSummary.java`
3. `end/src/main/java/com/metriclab/model/dto/ModelAnalysisResult.java`
4. `end/src/main/java/com/metriclab/model/dto/ModelAnalysisReportResult.java`
5. `end/src/main/java/com/metriclab/controller/ModelAnalysisController.java`
6. `end/src/main/java/com/metriclab/service/ModelAnalysisService.java`
7. `end/src/main/java/com/metriclab/storage/FileStorageService.java`
8. `end/src/main/java/com/metriclab/model/dto/FunctionPointRequest.java`
9. `end/src/main/java/com/metriclab/model/dto/UseCasePointRequest.java`
10. `end/src/main/java/com/metriclab/service/FunctionPointService.java`
11. `end/src/main/java/com/metriclab/service/UseCasePointService.java`
12. `end/src/main/java/com/metriclab/service/XmlExportService.java`
13. `end/src/main/java/com/metriclab/service/ComprehensiveReportService.java`
14. `front/src/services/api.js`
15. `front/src/App.vue`
16. `front/src/styles.css`
17. `项目开发进度记录.md`

已验证：

1. 后端执行 `mvn test` 成功。
2. 后端测试结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0。
3. 前端执行 `npm run build` 成功。
4. 前端 Vite 构建成功生成生产资源。

存在问题：

1. 模型文件解析覆盖常见 UML/XMI/XML 结构，但不同建模工具的 XMI 方言可能存在差异，后续可根据课堂样例继续补充适配规则。
2. 本次验证覆盖编译和构建，尚未进行浏览器端完整点击验收。
3. 豆包大模型真实调用仍需要运行后端并在页面发起智能分析请求才能验证实际服务返回。

下一步计划：

1. 启动前后端，按指导方案逐项上传 Java 源码和模型文件，完成页面端手工验收。
2. 使用真实火山方舟配置点击“AI 智能分析”，确认大模型返回内容可被解析并保存。
3. 如发现某类 XMI 文件无法识别，补充该建模工具对应的标签和属性解析规则。

### 2026-04-21 第 27 次记录

本次目标：

继续补齐完整验收清单中的管理类功能，包括项目编辑、历史任务查看、阈值配置和示例导入入口。

完成内容：

1. 后端新增项目更新接口，支持修改项目名称、语言和描述。
2. 后端新增项目历史任务接口，可读取 `tasks/{taskId}/task.json` 并返回任务类型、状态、创建时间和结果目录。
3. 后端新增阈值配置服务，支持读取和保存 `metric-data/config/thresholds.json`。
4. 后端新增阈值配置接口，支持前端获取和更新复杂度、耦合度、WMC、LCOM、注释率等阈值。
5. 前端项目管理页面新增项目编辑按钮和取消编辑按钮。
6. 前端项目详情卡片新增历史度量任务列表，展示最近任务和任务数量。
7. 前端项目管理页面新增指标阈值配置区域，可直接修改 low、medium、high 阈值并保存。
8. 前端“导入示例”按钮从禁用状态改为可用状态，可快速创建课堂演示项目。
9. 每次执行 LoC、圈复杂度、CK/LK、估算、功能点、用例点、模型文件和智能分析后，会刷新历史任务列表。
10. 删除“示例导入尚未实现”等明显未完成提示，避免验收时出现功能状态不一致。

修改文件：

1. `end/src/main/java/com/metriclab/controller/ProjectController.java`
2. `end/src/main/java/com/metriclab/controller/ConfigController.java`
3. `end/src/main/java/com/metriclab/service/ProjectService.java`
4. `end/src/main/java/com/metriclab/service/ThresholdConfigService.java`
5. `end/src/main/java/com/metriclab/storage/FileStorageService.java`
6. `end/src/main/java/com/metriclab/model/dto/TaskHistoryItem.java`
7. `end/src/main/java/com/metriclab/model/dto/UpdateProjectRequest.java`
8. `front/src/services/api.js`
9. `front/src/App.vue`
10. `front/src/styles.css`
11. `项目开发进度记录.md`

已验证：

1. 后端执行 `mvn test` 成功。
2. 后端测试结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0。
3. 前端执行 `npm run build` 成功。

存在问题：

1. 当前仍未做浏览器端逐项手工点击验收。
2. 当前真实豆包接口仍未在运行态发起请求验证。
3. Java 代码解析仍采用实验可用的规则解析方案，而不是完整 JavaParser/JDT ASTParser。

下一步计划：

1. 启动后端和前端，完成浏览器端完整操作验收。
2. 点击“AI 智能分析”，验证豆包真实返回是否能成功解析并保存。
3. 如需要进一步贴近指导方案建议，可继续接入 JavaParser 或补充最终实验报告、截图和人工计算对照表。

### 2026-04-21 第 28 次记录

本次目标：

排查智能分析回退到 `LocalRuleAnalyzer` 的原因，并提升豆包/火山方舟返回内容的解析兼容性。

完成内容：

1. 通过最小请求验证 `.env` 中的火山方舟 API Key、接口地址和模型名可以访问真实豆包服务。
2. 确认此前回退并非网络或密钥不可用，而是模型返回内容格式与原解析器预期不完全一致。
3. `AiAnalysisService` 新增大模型调用诊断日志，调用失败时会在后端控制台输出 HTTP 状态、截断后的响应或异常原因。
4. 大模型请求新增 `response_format: {"type":"json_object"}`，提高返回合法 JSON 的概率。
5. 强化系统提示词，要求模型只输出 JSON 对象，不输出 Markdown、解释或代码块。
6. 强化用户提示词，要求 `riskItems`、`refactoringSuggestions`、`testSuggestions` 的数组元素必须是字符串。
7. JSON 提取逻辑从“仅处理开头就是代码块”改为“从完整响应中截取第一个 `{` 到最后一个 `}`”，可兼容模型返回说明文字和 Markdown 代码块。
8. 数组解析逻辑从“只接受字符串数组”扩展为“字符串数组和对象数组都可解析”，对象数组会优先提取 `riskDescription`、`suggestionContent`、`testContent`、`description`、`content`、`reason` 等字段。
9. 解析失败时仍保留本地规则兜底，保证智能分析功能不会因大模型输出格式波动而中断。

修改文件：

1. `end/src/main/java/com/metriclab/service/AiAnalysisService.java`
2. `项目开发进度记录.md`

已验证：

1. 使用 `.env` 中的火山方舟配置发起最小请求，接口可正常返回内容。
2. 后端执行 `mvn test` 成功。
3. 后端测试结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0。

存在问题：

1. 需要重启后端后重新点击“AI 智能分析”，才能让本次解析增强生效。
2. 豆包模型仍可能生成带解释性文字的内容，但当前解析逻辑已能兼容大多数常见返回格式。
3. 大模型调用会消耗火山方舟额度，验收时建议只保留少量关键截图和记录。

下一步计划：

1. 用户重启后端，重新执行“AI 智能分析”。
2. 如果报告中的分析器显示 `doubao:doubao-seed-1-8-251228`，说明真实 AI 调用和解析均成功。
3. 如果仍显示 `LocalRuleAnalyzer`，查看后端控制台新增的大模型调用诊断日志继续定位。

### 2026-04-21 第 29 次记录

本次目标：

继续排查 `Large model analysis skipped: enabled=false` 问题，确保智能分析不再被大模型开关误拦截。

完成内容：

1. `.env` 加载逻辑新增 Spring Boot 原生属性名映射，将 `LARGE_MODEL_ENABLED` 同步写入 `metric.large-model.enabled`。
2. `.env` 加载逻辑同步映射 `ARK_API_KEY`、`ARK_MODEL`、`ARK_REASONING_EFFORT`、`ARK_MAX_COMPLETION_TOKENS` 等配置到 `metric.large-model.*`。
3. `AiAnalysisService` 启动时输出大模型配置诊断日志，包含 `enabled`、`effectiveEnabled`、模型名、Key 是否配置、BaseUrl 是否配置等非敏感信息。
4. 智能分析调用逻辑改为以 API Key、模型名和 BaseUrl 是否配置齐全作为有效启用条件。
5. 如果 `enabled=false` 但 API Key、模型名和 BaseUrl 已配置齐全，系统会继续调用大模型，并输出提示日志，避免实验环境变量误覆盖导致回退。

修改文件：

1. `end/src/main/java/com/metriclab/MetricLabApplication.java`
2. `end/src/main/java/com/metriclab/service/AiAnalysisService.java`
3. `项目开发进度记录.md`

已验证：

1. 后端执行 `mvn test` 成功。
2. 后端测试结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0。

存在问题：

1. `SpringBootTest` 不经过 `main()` 方法，因此测试启动日志不会读取 `.env`，这不影响用户通过 `mvn spring-boot:run` 的实际运行。
2. 需要用户重启后端后重新发起智能分析请求，确认启动日志中的 `effectiveEnabled=true`。

下一步计划：

1. 重启后端，观察启动日志中的 `Large model configuration loaded`。
2. 若 `effectiveEnabled=true`，直接调用“AI 智能分析”接口验证报告分析器是否变为 `doubao:doubao-seed-1-8-251228`。

### 2026-04-21 第 30 次记录
本次目标：
精简前端页面中重复的度量分析入口和重复请求逻辑，降低 `App.vue` 的维护复杂度，避免切换项目时一次性调用所有后端 latest 接口。

完成内容：
1. 前端新增 `metricModules` 度量模块配置表，统一维护 LoC、圈复杂度、CK/LK、COCOMO 估算、AI 分析、功能点、用例点和模型文件度量的 analyze/latest/report 入口。
2. 新增 `runMetricAnalysis` 通用分析函数，将多个 `runXxxAnalysis` 中重复的项目校验、loading、错误处理、结果保存和菜单切换逻辑合并。
3. 新增 `exportMetricMarkdown` 通用导出函数，将多个 Markdown 导出函数中的重复接口调用和下载逻辑合并。
4. 新增 `loadLatestMetricResult` 和 `loadLatestActiveMetricResult`，项目切换时只加载当前菜单对应的最新结果，不再一次性请求所有度量模块的 latest 接口。
5. 保留原有 `runLocAnalysis`、`runComplexityAnalysis` 等模板调用函数，但内部改为委托统一函数，避免大范围改动模板造成风险。
6. 新增 `resetMetricResults` 和 `markMetricResultsStale`，合并上传/删除文件后逐个模块设置提示信息的重复代码。
7. 移除右侧操作面板中与各度量页面顶部按钮重复的“开始分析”按钮组，保留 XML 导出和综合报告导出，减少同一后端功能的重复入口。
8. 移除右侧操作面板底部重复的 AI 智能分析快捷按钮和过期提示文案，避免与 AI 页面主按钮形成重复调用入口。

修改文件：
1. `front/src/App.vue`
2. `项目开发进度记录.md`

已验证：
1. 前端执行 `npm run build` 成功。
2. Vite 生产构建成功，生成 `dist/index.html`、CSS 和 JS 资源。

存在问题：
1. 本次主要完成前端逻辑瘦身和入口去重，尚未进一步拆分为多个 Vue 子组件。
2. `App.vue` 仍然较大，后续如需更专业的结构，可继续拆分 `ProjectManagement`、`MetricPanel`、`FileManager`、`ThresholdConfig` 等组件。

下一步计划：
1. 如需继续优化，可将 `metricModules` 相关逻辑抽到独立 composable，例如 `src/composables/useMetricModules.js`。
2. 可进一步把每个度量展示区域拆成独立组件，减少单文件模板长度。

### 2026-04-25 第 31 次记录

本次目标：

对当前项目进行一次结构化梳理，确认是否为前后端分离项目，整理当前已实现功能，并给出前端页面拆分与重构方案，作为后续继续优化 `App.vue` 的依据。

完成内容：

1. 确认项目采用前后端分离结构：
   - 前端目录为 `front`，技术栈为 `Vue 3 + Vite`。
   - 后端目录为 `end`，技术栈为 `Spring Boot 3 + Java 17`。
   - 前端通过 `/api` 调用后端接口，开发期由 Vite 代理到 `http://localhost:8080`。
   - 后端通过 `WebConfig` 为 `/api/**` 单独配置了 CORS。
2. 确认当前项目并非数据库驱动系统，而是“本地文件存储型软件度量平台”：
   - 项目索引保存在 `metric-data/projects.json`。
   - 阈值配置保存在 `metric-data/config/thresholds.json`。
   - 每个项目在 `metric-data/projects/<projectId>` 下保存上传文件、任务结果和报告。
3. 梳理当前已经具备的核心功能模块，包括：
   - 项目管理：新建、编辑、删除、选择项目。
   - 文件管理：按项目上传、删除、搜索、筛选和分页查看文件。
   - 历史任务查看：读取每个项目已执行的度量任务记录。
   - 阈值配置：维护复杂度、注释率、CBO、WMC、LCOM 等风险阈值。
   - LoC 代码行度量。
   - 圈复杂度分析。
   - 面向对象 CK/LK 度量。
   - 模型文件度量（`xml/xmi/oom`）。
   - 功能点估算。
   - 用例点估算。
   - COCOMO 工作量与成本估算。
   - AI 智能质量分析。
   - XML 导出与综合 Markdown 报告导出。
4. 对前端现状进行了可维护性评估，结论为“功能完整，但结构偏臃肿”：
   - `front/src/App.vue` 当前约 `2292` 行。
   - `front/src/styles.css` 当前约 `1280` 行。
   - 项目切换、文件上传、度量展示、导出、消息提示、筛选分页等逻辑集中在一个单文件组件中。
   - 文件管理区存在两套近似实现：一套位于项目管理页中的项目卡片内，另一套位于右侧操作面板中，存在明显重复。
   - 当前前端不是依赖太多或技术栈太复杂，而是“单文件过大、职责过多、重复 UI 过多”。
5. 给出前端拆分方向，明确后续重构应优先把 `App.vue` 拆成“壳层 + 视图层 + 公共组件层 + composables 层”。
6. 给出第一阶段建议目录结构：

```text
front/src
├─ app
│  ├─ AppShell.vue
│  ├─ TopBar.vue
│  ├─ SideMenu.vue
│  └─ PageHeader.vue
├─ views
│  ├─ ProjectManagementView.vue
│  ├─ LocAnalysisView.vue
│  ├─ ComplexityAnalysisView.vue
│  ├─ FunctionPointView.vue
│  ├─ UseCasePointView.vue
│  ├─ ModelAnalysisView.vue
│  ├─ ObjectOrientedView.vue
│  ├─ AiAnalysisView.vue
│  └─ EstimationView.vue
├─ components
│  ├─ project
│  │  ├─ ProjectForm.vue
│  │  ├─ ProjectList.vue
│  │  ├─ ProjectBoard.vue
│  │  ├─ ProjectCard.vue
│  │  ├─ TaskHistoryList.vue
│  │  └─ ThresholdConfigPanel.vue
│  ├─ file
│  │  ├─ ProjectFilePanel.vue
│  │  ├─ FileToolbar.vue
│  │  ├─ FileCategoryTabs.vue
│  │  ├─ FileList.vue
│  │  └─ FilePagination.vue
│  ├─ metric
│  │  ├─ MetricOverviewCards.vue
│  │  ├─ MetricActionHeader.vue
│  │  ├─ EmptyMetricState.vue
│  │  ├─ RiskBadge.vue
│  │  └─ ResultMessageGroup.vue
│  └─ common
│     └─ ExportActions.vue
├─ composables
│  ├─ useProjects.js
│  ├─ useProjectFiles.js
│  ├─ useMetricModules.js
│  ├─ useThresholdConfig.js
│  └─ useFormatters.js
└─ services
   ├─ projectApi.js
   ├─ fileApi.js
   ├─ metricApi.js
   └─ configApi.js
```

7. 明确第一阶段最值得优先拆分的模块顺序：
   - 第一步：优先抽离 `ProjectFilePanel.vue`，统一项目文件管理区与右侧操作面板中的重复文件列表、筛选、分页和删除逻辑。
   - 第二步：抽离 `MetricActionHeader.vue`、`ResultMessageGroup.vue`、`RiskBadge.vue` 等公共度量展示组件。
   - 第三步：把 LoC、复杂度、功能点、用例点、模型分析、面向对象、AI、估算等页面块拆成独立 `View`。
   - 第四步：将项目相关状态、文件相关状态、度量模块状态和阈值配置状态分别迁移到 composables 中。
8. 明确当前前端的核心问题不是“功能太多”，而是“多个合理功能被堆叠在一个组件里”，因此后续优化重点应放在职责拆分和代码复用，而不是删除已有模块。

修改文件：

1. `项目开发进度记录.md`

已验证：

1. 已检查项目根目录、`front/package.json`、`front/vite.config.js`、`front/src/App.vue`、`front/src/styles.css`、`end/pom.xml`、后端控制器与存储层代码。
2. 已确认前端 API 调用方式、开发代理配置、后端接口路径和本地文件存储结构。
3. 本次未执行 `npm run build` 或 `mvn test`，因为本次工作为结构分析与重构方案整理，未改动业务代码。

存在问题：

1. `App.vue` 目前仍然过大，后续只要继续增加功能，维护成本会继续上升。
2. 文件管理相关 UI 当前仍有两套近似实现，后续若继续修改筛选或分页逻辑，容易出现双处维护问题。
3. 目前仍未正式开始组件级拆分，因此这次记录提供的是“可落地的拆分路线”，尚未进入实际重构提交阶段。

下一步计划：

1. 如需继续执行重构，优先实现 `components/file/ProjectFilePanel.vue`，先消除文件管理区域的重复模板和重复逻辑。
2. 继续把度量页顶部通用操作区抽为公共组件，减少各模块模板重复。
3. 在公共组件抽离稳定后，再把 8 个度量页面拆为独立 `View`，逐步把 `App.vue` 缩减到可维护范围。

### 2026-04-25 第 32 次记录

本次目标：

开始执行前端第一阶段重构，优先抽离重复的文件管理区域，减少 `App.vue` 中两套文件列表、筛选、分页与上传交互模板的重复。

完成内容：

1. 新增前端公共组件：

```text
front/src/components/file/ProjectFilePanel.vue
```

该组件统一封装了以下文件管理 UI 能力：

1. 搜索文件名、类型和 ID。
2. 按上传时间、文件名、类型、大小排序。
3. 类型分类筛选。
4. 分页切换。
5. 文件列表渲染。
6. 删除文件事件透传。
7. 可选上传区域。
8. 可选刷新按钮和标题区。

2. `front/src/App.vue` 引入 `ProjectFilePanel.vue`，并将原本分散在两处的文件管理模板替换为公共组件调用：
   - 项目管理页中每个项目卡片内的文件管理区域。
   - 右侧操作面板中的“已上传文件”区域。
3. 新增 `displayProjectFiles(projectId)` 辅助函数，在父组件中统一补充文件大小和上传时间展示字段，避免把格式化逻辑散落到多个模板块中。
4. 保留原有的业务动作函数不变，包括：
   - `handleManagementFileChange`
   - `submitManagementUpload`
   - `removeManagementFile`
   - `handleFileChange`
   - `submitUpload`
   - `removeUploadedFile`
   - `updateFileState`
   - `changeFilePage`
   通过组件事件回调的方式复用，降低本次重构风险。
5. 本次重构后，`App.vue` 中两块重复的文件搜索、筛选、分页和列表模板已收敛为一个公共组件，后续如果要继续调整文件区 UI，只需优先修改 `ProjectFilePanel.vue`。

修改文件：

1. `front/src/components/file/ProjectFilePanel.vue`
2. `front/src/App.vue`
3. `项目开发进度记录.md`

已验证：

1. 前端执行 `npm run build` 成功。
2. Vite 生产构建成功，生成新的 `dist/index.html`、CSS 和 JS 资源。
3. 说明新增组件、事件绑定和模板替换均通过编译校验。

存在问题：

1. 当前只是完成“公共文件面板”一级拆分，`App.vue` 仍然很大，其他度量页面尚未拆成独立组件。
2. 文件上传消息和管理消息仍留在 `App.vue` 中，后续如果继续组件化，可再把消息区与操作头部一起抽象。
3. 项目管理页中的项目卡片本身仍然较长，后续可以继续拆出 `ProjectCard.vue` 和 `TaskHistoryList.vue`。

下一步计划：

1. 继续抽离度量页顶部的公共操作头，例如“标题 + 开始分析 + 导出 Markdown + 状态消息”区域。
2. 优先从 LoC、复杂度、估算等结构相近的页面开始拆成独立 `View`。
3. 在页面级拆分稳定后，再考虑把项目相关状态和文件相关状态迁移到 composables。

### 2026-04-25 第 33 次记录

本次目标：

继续执行前端第二阶段重构，抽离多个度量页面顶部重复的“标题区 + 主操作按钮 + 导出按钮 + 状态消息”模板，进一步降低 `App.vue` 的重复代码量。

完成内容：

1. 新增公共度量头部组件：

```text
front/src/components/metric/MetricActionHeader.vue
```

该组件统一封装了以下内容：

1. 页眉英文标识。
2. 页面中文标题。
3. 主操作按钮。
4. 主操作加载态文案。
5. Markdown 导出按钮。
6. 按钮点击事件透传。

2. 新增公共度量消息组件：

```text
front/src/components/metric/MetricStatusMessages.vue
```

该组件统一封装了以下内容：

1. 成功消息展示。
2. 错误消息展示。
3. 警告消息展示。
4. 过滤空消息，避免父组件重复编写多个 `v-if`。

3. `front/src/App.vue` 引入上述两个组件，并替换以下度量页面中重复的顶部结构：
   - LoC 代码行度量。
   - 圈复杂度分析。
   - 功能点度量。
   - 用例点估算。
   - 模型文件度量。
   - 面向对象度量。
   - AI 智能分析。
   - COCOMO 估算分析。
4. 复杂度分析页面的旧版结果警告信息改为通过 `MetricStatusMessages.vue` 统一渲染，减少页面级的单独提示模板。
5. 本次重构后，各度量页面顶部的重复模板已明显减少，后续如需统一修改“按钮排列、标题样式、消息展示方式”，只需优先修改公共组件即可。

修改文件：

1. `front/src/components/metric/MetricActionHeader.vue`
2. `front/src/components/metric/MetricStatusMessages.vue`
3. `front/src/App.vue`
4. `项目开发进度记录.md`

已验证：

1. 前端执行 `npm run build` 成功。
2. Vite 生产构建成功，说明新的公共度量组件在模板编译、属性传递和事件绑定上均工作正常。

存在问题：

1. 虽然度量页顶部重复代码已收敛，但每个度量页面的主体结果区仍然保留在 `App.vue` 中。
2. LoC、复杂度、模型、面向对象等表格展示区仍然较长，尚未拆成独立页面组件。
3. 当前重构仍以“低风险模板抽离”为主，状态管理和 composables 尚未开始拆分。

下一步计划：

1. 继续把 LoC、复杂度、估算等结构清晰的度量页面拆成独立 `View`。
2. 优先选择一个页面先完成整块迁移，验证页面级组件拆分方式后，再批量推进其他模块。
3. 在页面级拆分稳定后，再把 `metricModules`、项目状态和文件状态逐步迁移到 composables。

### 2026-04-25 第 34 次记录

本次目标：

在完成公共文件面板和公共度量头部之后，继续进行页面级拆分，选取结构最稳定的 LoC 页面作为第一个独立 `View` 迁移试点。

完成内容：

1. 新增页面级组件：

```text
front/src/views/LocAnalysisView.vue
```

2. `LocAnalysisView.vue` 内部封装了以下内容：
   - LoC 页面头部操作区。
   - 成功、报告和错误消息展示。
   - 空状态提示。
   - LoC 汇总指标卡片。
   - 文件级 LoC 结果表格。
3. `LocAnalysisView.vue` 复用了前一步新增的公共组件：
   - `MetricActionHeader.vue`
   - `MetricStatusMessages.vue`
4. `front/src/App.vue` 中原本属于 LoC 页面的大段模板已替换为：
   - 向 `LocAnalysisView.vue` 传入 `locResult`、`locLoading`、`locMessage`、`locReportMessage`、`locError`
   - 传入 `percent` 作为格式化函数
   - 通过事件回调保留 `runLocAnalysis` 和 `exportLocMarkdown`
5. 本次拆分验证了当前重构路线可行：
   - 公共组件先收敛重复结构。
   - 再把单个度量页整体迁移为独立 `View`。
   - 最终由 `App.vue` 只负责状态与事件分发，页面细节逐步迁出。

修改文件：

1. `front/src/views/LocAnalysisView.vue`
2. `front/src/App.vue`
3. `项目开发进度记录.md`

已验证：

1. 前端执行 `npm run build` 成功。
2. Vite 生产构建成功，说明页面级组件拆分后模板、属性传递和事件绑定均正常。

存在问题：

1. 当前只完成了 LoC 页面一级拆分，复杂度、模型分析、面向对象、估算等页面仍保留在 `App.vue` 中。
2. 当前页面仍通过 `App.vue` 统一持有状态，尚未迁移到 composables。
3. `App.vue` 虽然已经开始瘦身，但距离完全按页面拆分完成仍有较长工作量。

下一步计划：

1. 按照与 LoC 页面相同的方式，继续拆分复杂度分析页面。
2. 复杂度页面拆分稳定后，可继续迁移估算页或模型分析页。
3. 当多个页面完成迁移后，再统一梳理 `App.vue` 中剩余可抽离的状态和函数。

### 2026-04-25 第 35 次记录

本次目标：

继续按照 LoC 页面已验证的拆分方式，完成复杂度分析页面的页面级迁移，进一步减少 `App.vue` 中的单页模板堆积。

完成内容：

1. 新增页面级组件：

```text
front/src/views/ComplexityAnalysisView.vue
```

2. `ComplexityAnalysisView.vue` 内部封装了以下内容：
   - 复杂度页面头部操作区。
   - 成功、报告、错误和旧版结果警告消息展示。
   - 空状态提示。
   - 文件级扫描结果表格。
   - 方法级复杂度与风险等级表格。
   - “未发现可执行方法”提示区。
3. `ComplexityAnalysisView.vue` 继续复用公共组件：
   - `MetricActionHeader.vue`
   - `MetricStatusMessages.vue`
4. `front/src/App.vue` 中原本属于复杂度页面的大段模板已替换为对 `ComplexityAnalysisView.vue` 的调用：
   - 传入 `complexityResult`、`complexityLoading`、`complexityMessage`、`complexityReportMessage`、`complexityError`
   - 传入旧版结果警告消息
   - 传入 `riskLabel` 作为风险等级格式化函数
   - 通过事件回调保留 `runComplexityAnalysis` 与 `exportComplexityMarkdown`
5. 本次拆分进一步验证了当前页面级重构模式可以连续复用，后续可按相同方式继续拆模型分析页、估算页和面向对象页。

修改文件：

1. `front/src/views/ComplexityAnalysisView.vue`
2. `front/src/App.vue`
3. `项目开发进度记录.md`

已验证：

1. 前端执行 `npm run build` 成功。
2. Vite 生产构建成功，说明复杂度页面迁移后的属性传递、事件绑定和模板编译均正常。

存在问题：

1. `App.vue` 仍然保留功能点、用例点、模型分析、面向对象、AI、估算等多个大页面模板。
2. 虽然页面级模板已经开始迁移，但页面所需状态和动作函数目前仍集中在 `App.vue`。
3. 当前尚未引入路由，页面切换仍以 `activeMenu` 为核心控制方式。

下一步计划：

1. 继续优先拆分结构较清晰的估算页或模型分析页。
2. 在再迁移 1-2 个页面后，回头统一梳理 `App.vue` 中已经可以继续抽离的状态和通用函数。
3. 当页面级拆分达到一定数量后，再评估是否引入 `views + composables` 的进一步组织方式。

### 2026-04-25 第 36 次记录

本次目标：

继续按照已验证的页面级拆分模式，迁移 COCOMO 估算页，进一步减少 `App.vue` 中的表单与结果展示模板。

完成内容：

1. 新增页面级组件：

```text
front/src/views/EstimationView.vue
```

2. `EstimationView.vue` 内部封装了以下内容：
   - 估算页面头部操作区。
   - COCOMO 参数输入表单。
   - 成功、报告和错误消息展示。
   - 空状态提示。
   - 估算结果摘要卡片。
   - 模式、规模来源、人月成本和总成本结果区。
3. 为避免在子组件中直接修改父组件对象，新增了表单更新事件回传机制：
   - `EstimationView.vue` 通过 `update-form` 事件上报表单字段变更。
   - `App.vue` 新增 `updateEstimationForm(patch)`，统一在父层更新 `estimationForm`。
4. `front/src/App.vue` 中原本属于估算页的大段模板已替换为 `EstimationView.vue` 调用：
   - 传入 `estimationResult`、`estimationLoading`、`estimationMessage`、`estimationReportMessage`、`estimationError`
   - 传入 `estimationForm`
   - 通过事件回调保留 `runEstimationAnalysis` 与 `exportEstimationMarkdown`
5. 本次拆分表明，除了纯结果页之外，带输入表单的页面也可以按相同模式迁移到独立 `View`，后续拆功能点页、用例点页时也可参考本次做法。

修改文件：

1. `front/src/views/EstimationView.vue`
2. `front/src/App.vue`
3. `项目开发进度记录.md`

已验证：

1. 前端执行 `npm run build` 成功。
2. Vite 生产构建成功，说明估算页迁移后模板编译、事件回传与属性传递均正常。

存在问题：

1. `App.vue` 仍保留功能点、用例点、模型分析、面向对象、AI 等多个页面模板。
2. 页面模板虽然逐步迁出，但状态和动作函数仍集中在 `App.vue`，后续还需进一步整理。
3. 功能点页和用例点页包含更多因子录入表单，拆分时会比 LoC、复杂度和估算页稍复杂。

下一步计划：

1. 优先继续拆模型分析页或面向对象页，进一步减少大表格页面在 `App.vue` 中的占比。
2. 当再完成 1-2 个页面迁移后，开始评估把 `metricModules` 和部分页面状态迁移到 composables。
3. 在页面级拆分达到一定规模后，继续把项目管理区中的项目卡片和任务历史区抽为独立组件。

### 2026-04-25 第 37 次记录

本次目标：

继续执行页面级拆分，迁移模型分析页，进一步减少 `App.vue` 中的大表格展示模板。

完成内容：

1. 新增页面级组件：

```text
front/src/views/ModelAnalysisView.vue
```

2. `ModelAnalysisView.vue` 内部封装了以下内容：
   - 模型分析页面头部操作区。
   - 成功、报告和错误消息展示。
   - 空状态提示。
   - 模型文件数、类/接口数、属性/操作数、高风险类等摘要卡片。
   - 模型类/接口明细表格。
   - 风险等级展示。
3. `ModelAnalysisView.vue` 继续复用公共组件：
   - `MetricActionHeader.vue`
   - `MetricStatusMessages.vue`
4. `front/src/App.vue` 中原本属于模型分析页的大段模板已替换为 `ModelAnalysisView.vue` 调用：
   - 传入 `modelResult`、`modelLoading`、`modelMessage`、`modelReportMessage`、`modelError`
   - 传入 `riskLabel` 作为风险等级格式化函数
   - 通过事件回调保留 `runModelAnalysis` 与 `exportModelMarkdown`
5. 本次拆分后，LoC、复杂度、估算、模型分析四个页面已经迁出 `App.vue`，说明当前“公共组件 + 页面级 View”重构方式已经可以持续复用。

修改文件：

1. `front/src/views/ModelAnalysisView.vue`
2. `front/src/App.vue`
3. `项目开发进度记录.md`

已验证：

1. 前端执行 `npm run build` 成功。
2. Vite 生产构建成功，说明模型分析页迁移后的模板编译、属性传递和事件绑定均正常。

存在问题：

1. `App.vue` 仍保留功能点、用例点、面向对象、AI 等页面模板。
2. 虽然页面级模板已经继续减少，但页面数据状态与动作函数仍集中在 `App.vue` 中。
3. 面向对象页面包含雷达图和更复杂的可视化区域，后续拆分会比模型分析页略复杂。

下一步计划：

1. 继续优先拆分面向对象页面或 AI 页面。
2. 当再迁出 1-2 个页面后，开始整理 `App.vue` 中剩余的状态与通用动作，逐步迁移到 composables。
3. 在分析页拆分基本完成后，再回头处理项目管理区的卡片和任务历史组件化。

### 2026-04-25 第 38 次记录

本次目标：

继续执行页面级拆分，迁移面向对象度量页面，减少 `App.vue` 中包含可视化图形与大表格的复杂模板。

完成内容：

1. 新增页面级组件：

```text
front/src/views/ObjectOrientedView.vue
```

2. `ObjectOrientedView.vue` 内部封装了以下内容：
   - 面向对象页面头部操作区。
   - 成功、报告和错误消息展示。
   - 空状态提示。
   - Java 文件数、类/接口数、平均 CBO、风险类等摘要卡片。
   - CK/LK 指标说明区。
   - 雷达图展示区。
   - 类级指标明细表格。
   - 风险等级展示。
3. `ObjectOrientedView.vue` 通过属性接收：
   - `ooResult`
   - `ooLoading`
   - `ooMessage`
   - `ooReportMessage`
   - `ooError`
   - `radarAxes`
   - `radarSeries()`
   - `riskLabel`
4. `front/src/App.vue` 中原本属于面向对象页面的大段模板已替换为对 `ObjectOrientedView.vue` 的调用，父层只负责传递结果、雷达图数据和事件回调。
5. 本次拆分后，包含雷达图的可视化页面也已验证可以按同一模式迁移，说明后续 AI 页面、功能点页面和用例点页面也可以继续按“页面级 View + 公共组件”方式推进。

修改文件：

1. `front/src/views/ObjectOrientedView.vue`
2. `front/src/App.vue`
3. `项目开发进度记录.md`

已验证：

1. 前端执行 `npm run build` 成功。
2. Vite 生产构建成功，说明面向对象页面迁移后的模板编译、数据传递和雷达图展示结构均正常。

存在问题：

1. `App.vue` 仍保留功能点、用例点、AI 等页面模板。
2. 尽管页面模板继续减少，但 `App.vue` 中的状态和动作函数仍较多，后续应开始迁移到 composables。
3. 功能点页和用例点页包含较多参数输入项，拆分复杂度会高于 LoC、复杂度、模型分析和估算页。

下一步计划：

1. 继续优先拆分 AI 页面或功能点页面。
2. 当页面级拆分基本完成后，开始整理 `metricModules`、项目状态和文件状态，逐步迁移到 composables。
3. 在分析页拆分稳定后，再回头继续拆项目管理区和任务历史区域。

### 2026-04-25 第 39 次记录

本次目标：

继续执行页面级拆分，迁移 AI 智能分析页面，进一步减少 `App.vue` 中的结果展示模板和列表渲染逻辑。

完成内容：

1. 新增页面级组件：

```text
front/src/views/AiAnalysisView.vue
```

2. `AiAnalysisView.vue` 内部封装了以下内容：
   - AI 页面头部操作区。
   - 成功和错误消息展示。
   - 空状态提示。
   - 总体评价卡片。
   - 主要风险列表。
   - 重构建议列表。
   - 测试建议列表。
3. `AiAnalysisView.vue` 通过属性接收：
   - `aiResult`
   - `aiLoading`
   - `aiMessage`
   - `aiError`
   - `formatDate`
4. `front/src/App.vue` 中原本属于 AI 页面的大段模板已替换为 `AiAnalysisView.vue` 调用，父层只负责传递结果、时间格式化函数和事件回调。
5. 本次拆分后，LoC、复杂度、估算、模型分析、面向对象、AI 六个页面已迁出 `App.vue`，说明当前的页面级重构方式已经覆盖了表格页、表单页、可视化页和建议列表页等多种场景。

修改文件：

1. `front/src/views/AiAnalysisView.vue`
2. `front/src/App.vue`
3. `项目开发进度记录.md`

已验证：

1. 前端执行 `npm run build` 成功。
2. Vite 生产构建成功，说明 AI 页面迁移后的模板编译、属性传递和事件绑定均正常。

存在问题：

1. `App.vue` 仍保留功能点页和用例点页等较长表单页面模板。
2. 状态、动作函数以及 `metricModules` 仍集中在 `App.vue` 中，后续应逐步迁移到 composables。
3. 当前页面切换仍基于 `activeMenu`，还未进一步引入页面级路由组织。

下一步计划：

1. 继续拆分功能点页和用例点页，进一步减少 `App.vue` 的剩余模板体量。
2. 当页面级拆分接近完成后，开始抽离 `metricModules` 和页面状态到 composables。
3. 在分析页拆分基本结束后，再回头继续拆项目管理区与任务历史相关组件。

### 2026-04-25 第 40 次记录

本次目标：

继续执行页面级拆分，迁移功能点页和用例点页两块长表单页面，尽可能清空 `App.vue` 中剩余的大段分析页模板。

完成内容：

1. 新增页面级组件：

```text
front/src/views/FunctionPointView.vue
front/src/views/UseCasePointView.vue
```

2. `FunctionPointView.vue` 内部封装了以下内容：
   - 功能点页面头部操作区。
   - EI、EO、EQ、ILF、EIF 数量录入表单。
   - 14 项 GSC 因子录入区。
   - 成功、报告和错误消息展示。
   - 空状态提示。
   - UFP、VAF、AFP、GSC 总分等结果区。
3. `UseCasePointView.vue` 内部封装了以下内容：
   - 用例点页面头部操作区。
   - 参与者复杂度录入区。
   - 用例复杂度录入区。
   - 技术因子与环境因子录入区。
   - 成功、报告和错误消息展示。
   - 空状态提示。
   - UCP、工时、人月、UUCP 以及 UAW、UUCW、TCF、ECF 结果区。
4. 为了保持低风险，功能点页和用例点页均采用“子组件上报字段变更，父组件统一更新状态”的方式：
   - `App.vue` 新增 `updateFunctionPointCount`
   - `App.vue` 新增 `updateFunctionPointGsc`
   - `App.vue` 新增 `updateUseCaseField`
   - `App.vue` 新增 `updateUseCaseTechnical`
   - `App.vue` 新增 `updateUseCaseEnvironmental`
5. `front/src/App.vue` 中原本属于功能点页和用例点页的大段模板已替换为两个独立 `View` 调用。
6. 本次拆分完成后，LoC、复杂度、功能点、用例点、估算、模型分析、面向对象、AI 八个分析页面均已迁出 `App.vue`。

修改文件：

1. `front/src/views/FunctionPointView.vue`
2. `front/src/views/UseCasePointView.vue`
3. `front/src/App.vue`
4. `项目开发进度记录.md`

已验证：

1. 前端执行 `npm run build` 成功。
2. Vite 生产构建成功，说明功能点页和用例点页迁移后的模板编译、表单更新事件和结果展示均正常。

存在问题：

1. 尽管分析页模板已基本迁出，但 `App.vue` 仍持有较多状态、动作函数和 `metricModules` 配置。
2. 项目管理页、项目卡片、任务历史区和阈值配置区仍未进一步组件化。
3. 当前代码组织已从“超大模板”改善为“多 View + 单一状态中心”，下一阶段更适合整理状态层而不是继续堆页面拆分。

下一步计划：

1. 开始抽离 `metricModules` 相关状态与通用动作到 composables。
2. 继续拆项目管理区中的项目卡片、任务历史区和阈值配置区。
3. 在状态和组件继续稳定后，再评估是否引入更明确的 `views + composables + domain components` 目录组织。

### 2026-04-25 第 41 次记录

本次目标：

在分析页模板基本迁移完成后，开始进入状态层整理阶段，优先抽离 `metricModules` 相关状态、表单状态和通用分析/导出动作到 composable。

完成内容：

1. 新增 composable：

```text
front/src/composables/useMetricModules.js
```

2. `useMetricModules.js` 中统一收纳了以下内容：
   - LoC、复杂度、面向对象、估算、AI、功能点、用例点、模型分析等页面的结果状态。
   - 对应的 loading、message、error、reportMessage 状态。
   - `estimationForm`、`functionPointForm`、`useCasePointForm` 三组分析页表单状态。
   - `functionPointGscTotal`、`useCaseTechnicalTotal`、`useCaseEnvironmentalTotal` 等表单汇总函数。
   - `metricModules` 配置表。
   - `resetMetricResults`
   - `markMetricResultsStale`
   - `loadLatestMetricResult`
   - `loadLatestActiveMetricResult`
   - `runMetricAnalysis`
   - `exportMetricMarkdown`
   - 各页面的 `runXxxAnalysis` / `exportXxxMarkdown` 封装函数。
3. `useMetricModules.js` 内部直接引入分析相关 API，使分析页的状态逻辑与接口调用更加集中，不再散落在 `App.vue` 顶部的大量 import 中。
4. `front/src/App.vue` 中原本属于 `metricModules` 的大块状态与通用分析逻辑已替换为从 `useMetricModules()` 解构获取：
   - 分析结果状态。
   - 分析页表单状态。
   - 通用分析/导出函数。
   - 通用结果重置与过期提示函数。
5. 本次重构后，`App.vue` 的职责开始从“状态中心 + 页面模板 + 通用分析动作”收缩为“页面装配 + 项目/文件管理 + 少量表单字段更新”，为后续继续抽离项目管理区和文件域状态打下基础。

修改文件：

1. `front/src/composables/useMetricModules.js`
2. `front/src/App.vue`
3. `项目开发进度记录.md`

已验证：

1. 前端执行 `npm run build` 成功。
2. Vite 生产构建成功，说明 composable 抽离后的状态传递、分析动作和页面调用均正常。

存在问题：

1. `App.vue` 仍然保留较多项目管理、文件管理和表单更新函数。
2. 分析页状态已经抽离，但项目域和文件域状态尚未迁移到 composables。
3. 项目管理页中的项目卡片、任务历史区和阈值配置区仍然没有进一步组件化。

下一步计划：

1. 继续抽离项目管理和文件管理相关状态到 composables，例如 `useProjects`、`useProjectFiles`。
2. 拆分项目管理区中的项目卡片、任务历史区和阈值配置区。
3. 在状态层和组件层进一步稳定后，再统一评估 `App.vue` 的剩余职责是否还可以继续压缩。

### 2026-04-25 第 42 次记录

本次目标：

继续执行状态层整理，优先抽离文件管理域状态与操作到 composable，减少 `App.vue` 中与上传、删除、筛选、分页相关的大块逻辑。

完成内容：

1. 新增 composable：

```text
front/src/composables/useProjectFiles.js
```

2. `useProjectFiles.js` 中统一收纳了以下内容：
   - `uploadedFiles`
   - `selectedFile`
   - `projectFileMap`
   - `managementSelectedFiles`
   - `managementUploadLoading`
   - `managementMessage`
   - `managementError`
   - `uploadLoading`
   - `uploadMessage`
   - `uploadError`
   - `loadAllProjectFiles`
   - `loadFilesForProject`
   - `loadProjectFiles`
   - `handleFileChange`
   - `handleManagementFileChange`
   - `submitUpload`
   - `submitManagementUpload`
   - `removeUploadedFile`
   - `removeManagementFile`
   - `projectFiles`
   - `displayProjectFiles`
   - `fileState`
   - `updateFileState`
   - `fileCategories`
   - `filteredProjectFiles`
   - `pagedProjectFiles`
   - `pageCount`
   - `currentFilePage`
   - `changeFilePage`
3. `useProjectFiles.js` 内部直接引入文件相关 API，把文件缓存、上传、删除、搜索、排序、分页等逻辑从 `App.vue` 中集中抽离出来。
4. `front/src/App.vue` 中原本属于文件管理的大段状态与函数已替换为从 `useProjectFiles()` 解构获取。
5. 本次重构后，`App.vue` 中与文件域相关的职责明显减少，结构开始向“项目域 + 页面装配 + 少量表单更新”的方向收缩。

修改文件：

1. `front/src/composables/useProjectFiles.js`
2. `front/src/App.vue`
3. `项目开发进度记录.md`

已验证：

1. 前端执行 `npm run build` 成功。
2. Vite 生产构建成功，说明文件域状态抽离后的上传、删除、筛选和页面引用均通过编译校验。

存在问题：

1. `App.vue` 仍保留较多项目管理、任务历史、阈值配置和项目表单更新逻辑。
2. 项目管理区中的项目卡片、任务历史和阈值配置仍未拆成独立组件。
3. 项目域状态尚未迁移到 `useProjects` 一类 composable 中。

下一步计划：

1. 继续抽离项目管理相关状态与操作到 `useProjects` composable。
2. 拆分项目管理区中的项目卡片、任务历史区和阈值配置区。
3. 在项目域状态抽离完成后，再统一评估 `App.vue` 的剩余职责和后续目录组织方式。

### 2026-04-25 第 43 次记录
本次目标：
针对“前端页面看起来繁多而且偏丑”的问题，暂停继续做机械式代码拆分，转而直接优化信息架构和视觉层级，优先改造左侧导航和项目管理页。

完成内容：
1. 调整左侧导航结构：
   - 将原先一整列平铺的 9 个菜单重组为 4 个分组：
     - 工作台
     - 代码质量
     - 规模与估算
     - 报告与建议
   - 新增导航说明区和建议操作顺序提示，降低首次进入时的杂乱感。
2. 重构 `front/src/App.vue` 中的项目管理页信息布局：
   - 将原来“所有项目全部展开”的项目文件管理区改为“当前项目工作台”模式。
   - 页面顶部新增当前项目、累计文件、累计任务三张概览卡片。
   - 保留项目创建/编辑表单，但将项目切换区改成紧凑列表。
   - 项目文件上传、历史任务、项目统计只对当前项目展开，不再对每个项目都渲染一整块详细卡片。
   - 新增“其他项目”轻量列表，用于快速切换项目，避免再次出现整页堆满项目卡片的情况。
3. 在 `front/src/App.vue` 中补充了辅助状态：
   - `currentProject`
   - `totalProjectFiles`
   - `totalProjectTasks`
   - `inactiveProjects`
   这些计算属性用于支撑新的“当前项目优先”布局。
4. 升级 `front/src/styles.css` 的整体视觉表现：
   - 更新整体背景、顶栏和侧边栏配色，弱化课程作业式的灰蓝后台感。
   - 优化按钮、卡片、列表、工作区面板的圆角、阴影和层级。
   - 新增项目概览区、导航分组区、其他项目列表等样式。
   - 保持移动端和窄屏断点下的响应式适配。

修改文件：
1. `front/src/App.vue`
2. `front/src/styles.css`
3. `项目开发进度记录.md`

已验证：
1. 前端执行 `npm run build` 成功。
2. Vite 生产构建通过，说明本次导航重组、项目管理页重排和样式调整后的模板与脚本均可正常编译。

效果判断：
1. 页面当前最明显的变化是“少展开、强主次”，项目管理页不再把所有项目的任务和文件同时铺满页面。
2. 左侧导航从“单列堆叠入口”变为“分组导航”，视觉压力明显减轻。
3. 当前前端仍然功能较多，但已经从“内容全堆在同一层”改善为“当前项目优先、其他信息后置”的结构。

存在问题：
1. 当前分析页虽然已经比以前清晰，但整体仍属于功能型后台，不是完全轻量化的产品首页。
2. 功能点、用例点等长表单页面仍然信息密度偏高，后续可以进一步改为折叠区或分步输入。
3. `App.vue` 虽然职责已明显收缩，但项目域状态仍可继续抽到 `useProjects` 中。

下一步计划：
1. 继续做“减法式”前端优化，而不是回到纯拆文件节奏。
2. 优先改造功能点、用例点等长表单页的视觉层级与输入方式。
3. 视需要再继续抽离项目域状态到 `useProjects`，让结构和界面改造同步推进。

### 2026-04-26 第 44 次记录
本次目标：
处理“页面 AI 味太重、很多文字意义不明”的问题，压缩装饰性文案，把页面说明改为直接、可执行的界面语言。

完成内容：
1. 精简 `front/src/App.vue` 中项目管理页的说明文案：
   - 将“Focused Workspace”“Current Project Workspace”“Create or Update”“Risk Thresholds”等英文或展示式标题改为直接中文。
   - 将“当前项目优先”“把项目创建、项目切换和文件管理拆成主次区域……”这类解释型文案改为简短状态说明。
   - 将“当前工作区只展开一个项目，其余项目保持紧凑显示”“保留轻量列表，避免再次出现整页展开”等偏说明书式文字改为更短的操作提示。
2. 精简侧边栏说明区：
   - 原先的“先选项目，再做度量”“建议顺序：上传文件 → 运行度量 → 导出报告”等带有明显生成式语气的提示被改成更朴素的界面信息。
   - 侧边栏顶部改为显示本地项目数量和当前选中项目，降低无效说明占比。
3. 统一页面副标题风格：
   - `activeEyebrow()` 返回值从中英混杂、偏展示化的表达改为直接的中文功能标签，例如“项目与文件”“代码行统计”“复杂度分析”“工作量与成本”等。

修改文件：
1. `front/src/App.vue`
2. `项目开发进度记录.md`

已验证：
1. 前端执行 `npm run build` 成功。
2. Vite 生产构建通过，说明本次文案收缩未影响模板编译和页面结构。

效果判断：
1. 页面从“解释自己为什么这样布局”改成了“直接告诉用户当前是什么、能做什么”。
2. 英文口号、混合风格副标题和意义不强的提示语明显减少，界面更像系统页面而不是展示稿。

存在问题：
1. 项目管理页的结构已经比之前清楚，但视觉上仍然偏“重卡片”。
2. 分析页内部还有一些说明性小字，后续仍可继续做减法。

下一步计划：
1. 继续清理各分析页里的冗余说明文字。
2. 视需要再把卡片密度和留白做一轮收缩，让页面更朴素、更像工具而不是海报。

### 2026-04-26 第 45 次记录
本次目标：
按“页面简洁、功能清晰、没有废话、设计简约、组件能随页面缩放自适应变化”的要求，继续做一轮前端减法式改造。

完成内容：
1. 继续清理 `front/src/App.vue` 中的冗余界面文案：
   - 删除左侧栏中不提供实际信息的提示块。
   - 删除分析页顶部统计卡片中的说明小字，只保留标题和值。
   - 删除项目管理页中“点击项目可切换当前工作区”等重复解释性文字。
   - 将导航分组中的“报告与建议”改为更直接的“智能分析”。
2. 精简多个分析页标题和空状态文案：
   - `LocAnalysisView.vue`
   - `ComplexityAnalysisView.vue`
   - `FunctionPointView.vue`
   - `UseCasePointView.vue`
   - `EstimationView.vue`
   - `ObjectOrientedView.vue`
   - `AiAnalysisView.vue`
   - `ModelAnalysisView.vue`
   将原有偏展示化、英文较多的副标题改为简短中文。
3. 删除功能点页、用例点页中多余的公式说明段落：
   - 保留表单、结果和总分，不再在界面中重复输出解释性小作文。
4. 重做 `front/src/styles.css` 的整体视觉方向：
   - 去掉大面积渐变、玻璃感和厚阴影，改回更朴素的白底浅灰工具界面。
   - 统一卡片、面板、按钮的圆角与边框，减少“展示页”气质。
   - 降低卡片高度和留白密度，避免页面显得臃肿。
5. 强化响应式与自适应布局：
   - 将多个固定列布局改为 `repeat(auto-fit, minmax(...))`：
     - 项目概览区
     - 项目主工作区
     - 次级区域
     - 指标概览卡片
     - LoC/估算等结果卡片
     - 功能点、用例点、阈值、因子录入区
   - 将分析页主布局从固定 `1fr + 340px` 改为更柔性的主区 + 侧栏，并在中等宽度下提前自动改为单列，避免组件在窄屏下被挤压。

修改文件：
1. `front/src/App.vue`
2. `front/src/styles.css`
3. `front/src/components/metric/MetricActionHeader.vue`
4. `front/src/views/LocAnalysisView.vue`
5. `front/src/views/ComplexityAnalysisView.vue`
6. `front/src/views/FunctionPointView.vue`
7. `front/src/views/UseCasePointView.vue`
8. `front/src/views/EstimationView.vue`
9. `front/src/views/ObjectOrientedView.vue`
10. `front/src/views/AiAnalysisView.vue`
11. `front/src/views/ModelAnalysisView.vue`
12. `项目开发进度记录.md`

已验证：
1. 前端执行 `npm run build` 成功。
2. Vite 生产构建通过，说明本次模板精简、样式收缩和响应式改造均通过编译校验。

效果判断：
1. 页面整体从“多说明、多卡片、多装饰”进一步收缩为“标题 + 数据 + 操作”的工具界面。
2. 宽屏下组件会更自然地换列，窄屏下会更早进入单列，不容易出现内容被挤成一团的情况。
3. 英文标题、解释性小字和展示感较强的视觉元素明显减少，界面风格更克制。

存在问题：
1. 当前前端已经明显简化，但分析页内部仍然保留不少表格和录入区，信息密度仍高于真正轻量化产品。
2. 项目管理和分析页虽然更简洁了，但还没有做到“按任务流逐步展开”的极简交互。

下一步计划：
1. 继续压缩功能点、用例点等长表单页的输入密度。
2. 视需要把部分次要区域改成折叠区，继续减少同屏信息量。

### 2026-04-26 第 46 次记录
本次目标：
处理“页面上方不要空着，希望有一些 mock 来展示各个功能”的需求，在项目管理页顶部增加功能预览区。

完成内容：
1. 在 `front/src/App.vue` 中新增 `mockFeatureCards` 数据：
   - 覆盖项目管理、代码行度量、复杂度分析、面向对象、模型文件、功能点、用例点、智能分析等主要功能。
   - 每张卡片包含功能名称、分类标签、两项 mock 指标和简短的条形预览。
2. 在项目管理页顶部新增“功能预览”区：
   - 使用一组 mock 卡片直接展示平台有哪些主要能力。
   - 每张卡片都可以点击，直接切换到对应功能页面。
   - 页面顶部不再只有概览数字，而是先能看到完整功能面貌。
3. 在 `front/src/App.vue` 中新增 `openFeature(featureKey)`：
   - 用于点击 mock 卡片后切换到对应模块。
   - 对分析页仍保持读取最新结果的逻辑。
4. 在 `front/src/styles.css` 中补充功能预览区样式：
   - 新增 `feature-preview-section`
   - 新增 `feature-preview-grid`
   - 新增 `feature-preview-card`
   - 新增顶部标签、统计信息和 mock 条形图样式
   - 预览区采用 `repeat(auto-fit, minmax(...))`，可随屏幕宽度自动换列

修改文件：
1. `front/src/App.vue`
2. `front/src/styles.css`
3. `项目开发进度记录.md`

已验证：
1. 前端执行 `npm run build` 成功。
2. Vite 生产构建通过，说明 mock 功能预览区模板、事件和样式均可正常编译。

效果判断：
1. 项目管理页顶部不再显得空，进入页面后可以直接看到平台支持的核心功能。
2. mock 区既起到展示作用，也起到快捷导航作用，不是纯装饰内容。
3. 预览卡片会根据宽度自动换列，避免堆成固定大块。

存在问题：
1. 当前 mock 仍是静态预览，不会随着真实结果联动变化。
2. 分析页内部本身还没有类似的预览块，主要改善集中在项目管理页顶部。

下一步计划：
1. 视需要把 mock 预览逐步替换为“半真实摘要”，让顶部卡片能读取真实项目状态。
2. 继续优化分析页的首屏信息密度和层级。

### 2026-04-26 第 47 次记录
本次目标：
针对“前端内容还是太多、AI 风仍然明显”的问题，执行一轮更强的内容减法，把非必要内容从默认展开改为默认隐藏。

完成内容：
1. 删除项目管理页顶部的 mock 功能预览区：
   - 移除 `mockFeatureCards`
   - 移除 `openFeature(featureKey)`
   - 删除项目页顶部的“主要功能”预览卡片整块
   这样页面进入后不再先看到一组展示性质很强的 mock 卡。
2. 删除分析页顶部的全局概览卡：
   - 移除 `App.vue` 中原本在分析页顶部展示的 4 张统计卡
   - 同时移除不再使用的 `metricCardValue()` 和 `metricLabel()`
   现在分析页进入后直接看到当前分析内容，而不是先看一层摘要卡。
3. 收缩项目页默认信息量：
   - 历史任务区只显示最近 3 条，避免同屏堆太多历史记录
   - 阈值配置区改为默认折叠，点击“展开”后才显示详细配置
   - “其他项目”改为默认折叠，点击“展开”后才显示切换列表
4. 更新样式配合新的减法布局：
   - 删除功能预览区相关样式
   - 删除全局指标卡相关样式
   - 新增折叠区 `section-toggle-panel` 的简洁样式

修改文件：
1. `front/src/App.vue`
2. `front/src/styles.css`
3. `项目开发进度记录.md`

已验证：
1. 前端执行 `npm run build` 成功。
2. Vite 生产构建通过，说明这次删除 mock、删除分析页概览卡和新增折叠区后的模板均可正常编译。

效果判断：
1. 项目管理页首屏明显变短，进入页面后不再先看到一排 mock 卡。
2. 分析页从“概览卡 + 正文”改成“直接正文”，更接近工具使用场景。
3. 阈值配置和其他项目不再默认占用页面空间，页面主次关系更清楚。

存在问题：
1. 项目页虽然已经收缩，但仍然同时包含项目概览、表单、项目列表、当前项目详情等多个区域。
2. 功能点、用例点等分析页本身的输入区仍然比较长，后续还可以继续折叠化。

下一步计划：
1. 继续把长表单页做成分组折叠，减少一次性展示的输入项。
2. 视需要进一步压缩项目管理页顶部的概览区。

### 2026-04-26 第 48 次记录
本次目标：
继续压缩长表单页的首屏长度，把功能点页和用例点页改成“核心输入默认显示，高级因子折叠展开”的形式。

完成内容：
1. 改造 `front/src/views/FunctionPointView.vue`：
   - 保留 EI、EO、EQ、ILF、EIF 和汇总区作为默认显示内容。
   - 将原本整块展开的 `14 项通用系统特征 GSC` 改为折叠区。
   - 折叠标题直接显示 `GSC` 当前总分，便于不展开时也能看到状态。
2. 改造 `front/src/views/UseCasePointView.vue`：
   - 保留参与者复杂度、用例复杂度、汇总区作为默认显示内容。
   - 将 `13 项技术复杂度因子 TCF` 改为折叠区。
   - 将 `8 项环境复杂度因子 ECF` 改为折叠区。
   - 两个折叠标题都直接显示当前总分，避免用户必须展开后才能了解当前输入状态。
3. 更新 `front/src/styles.css`：
   - 新增 `form-section-toggle` 折叠区样式。
   - 新增 `summary`、展开/收起标识和总分徽标样式。
   - 新增 `factor-score-panel.embedded` 样式，保证折叠内容展开后仍保持紧凑布局。

修改文件：
1. `front/src/views/FunctionPointView.vue`
2. `front/src/views/UseCasePointView.vue`
3. `front/src/styles.css`
4. `项目开发进度记录.md`

已验证：
1. 前端执行 `npm run build` 成功。
2. Vite 生产构建通过，说明两处折叠区模板和样式均可正常编译。

效果判断：
1. 功能点页和用例点页首屏明显缩短，不再一进入页面就看到大块因子输入表。
2. 当前版本更接近“先做主要输入，必要时再展开高级参数”的工具交互。

存在问题：
1. 这两个页面虽然已收缩，但核心输入区本身仍然偏长。
2. 其余分析页中的表格和摘要区仍然可能偏多，后续还可以继续做减法。

下一步计划：
1. 继续评估是否要把更多结果区改成默认摘要 + 按需展开明细。
2. 进一步压缩项目管理页和分析页中非核心辅助信息。
